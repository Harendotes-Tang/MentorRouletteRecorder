# 变更记录 / Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [1.4.0] - 2026-09-21

### 新增
- **不启动游戏也能识别游戏版本。**此前客户端版本只能在游戏运行时从其目录中的 `ffxivgame.ver` 读取，游戏未启动时界面只能显示「待游戏启动后校验」，档案匹配与共享校准的获取都要等到进入游戏之后。现在采集服务会记住上次见到的游戏安装目录（数据目录下的 `game-install.json`，只保存在本机，不进入界面、日志、诊断报告与导出文件），软件启动时直接从该目录读取已安装的版本号：协议档案随即按该版本匹配；该版本没有可用档案时，立即获取其他玩家的共享校准，不必等到登录之后（`docs/privacy-boundary.md` §8.2 的触发条件相应由「游戏正在运行」放宽为「客户端版本已知」，请求本身与频率限制不变）。不新增任何联网请求，也不新增任何对游戏进程的访问。
- 捕获诊断页的链路在游戏未运行时显示「已安装版本」，协议档案一栏按该版本给出真实状态（档案匹配 / 本机校准 / 共享校准 / 待校准等），不再一律显示「待游戏启动」。
- **重新校准做错了可以一键换回去。**停用一份本机校准之后，如果发现新换的不如原来那份——比如先下载到的共享校准是按排本推断的，弹窗不再报话——以前没有任何办法换回去，那份档案还躺在磁盘上，只是换了个名字。现在「协议档案」卡上会出现「恢复上一份本机校准」：确认后把上次停用的那份换回来并立即用它记录，正在使用的共享校准只是被让位（不算被证伪，校准码不被拒绝），之前的记录都不受影响。被软件自己发现认错而撤下的档案不在此列：那不是你选择停用的，明天它读到的仍是同一条错报文。副本进行中时该按钮同样暂不可用。
- **本机校准出错时可以「重新校准」。**本机校准出来的档案一旦生效，校准卡片就会消失，「清空进度并重新观察」与「导入校准码」随之一并不可用；怀疑软件认错了报文、或拿到了其他玩家更好的校准码的人，此前只能自己去文件夹里改文件名。现在捕获页的「协议档案」卡上多一个「重新校准」：确认后停用当前的本机校准（文件改名保留，不删除），软件回到观察状态，之后可以重新校准，也可以导入其他玩家的校准码。之前的记录不受影响，也不会被标为待复核——是你要求重新校准，而不是软件发现它错了。副本进行中时该按钮暂不可用，以免白打这一把。

### 变更
- **读取游戏安装路径时不再有「打开游戏进程句柄」这条备用途径。**软件一直承诺从不打开游戏进程句柄、从不读取游戏进程内存，安装路径也一直优先从内核进程表读取（这条途径不需要对游戏进程的任何权限，国服启动器以管理员身份拉起的客户端同样读得到）。但此前还留着一条备用途径：内核进程表没有给出结果时，改用 .NET 的 `Process.MainModule` 读取，而这个属性内部正是打开游戏进程的句柄、再读取它的模块表——与上面那句承诺恰好相反；静态边界检查按 `OpenProcess` 一类的名字逐行匹配，看不见这种经由框架属性发生的同一件事。现在这条备用途径已经删除：内核进程表读不到，安装路径就保持未知，软件照常按「认不出客户端版本就不记录」处理，不提权、不重试、不推测。静态边界检查同时补上了针对这类写法的规则，并把安装脚本与持续集成配置一并纳入扫描范围。诊断文档里基于该备用途径的「必要时以管理员身份运行一次」也一并去掉了：路径取自内核进程表，提权对此没有帮助。详见 `docs/privacy-boundary.md` 第 2 节规则 3b。
- **数据库损坏时，软件如实说明无法启动，并告诉你数据库文件在哪里。**数据库文件校验不通过、版本高于本程序所支持的版本，或迁移记录被改动时，此前的提示说「已进入只读模式」，文档也这样写——但本软件从来没有只读模式：采集服务会直接退出，界面既看不到历史，也用不了设置里的备份。现在提示直接说明本软件无法启动，给出数据库文件的完整位置，并建议先把它复制一份保存到别处再排查；相关文档一并改为如实描述。
- 维护相关：持续集成与共享校准公开仓库的全部 GitHub Actions 改为按提交 id 固定引用，不再跟随可以被改写的标签（其中 Qt 安装那一项此前引用的甚至是一个随每次推送移动的分支）；共享校准仓库读取待处理 Issue 清单时加上 16 MiB 上限，超限时明确报错；副本名称数据生成工具在分页没有取完时不再静默产出缺条目的数据；持续集成所用的两个 Python 依赖改由依赖文件管理，纳入自动升级与漏洞跟踪。
- **主按钮不再与「已选中」同色。**艾欧泽亚与 Harendotes 两种界面风格下，主按钮与分段选择器里已选中的那一项用的是同一块实心底板，「手动修正」挨着「软删除」、「通关」挨着「未通关」时，看起来像是已经被选中的一项，而不是一个可以点的按钮。现在实心底板只表示「已选中」；主按钮平时是淡色底板加亮色边框，指针移上去或按下时才填实。经典风格不受影响。
- **总览的「导随总次数」改名为「已记录次数」。**该数字只统计本软件记录到的次数，不含手动填写的已有基数，原名容易被理解为生涯总次数。
- **首次运行说明更新了「联网一」的措辞，升级后会再显示一次。**正在使用的校准来自其他玩家、或者只是按排本推断出来的时候，软件会再读一次公开的校准列表，用来发现它已被撤回、或者有了更准的一份；原先的说明写的是「已经有可用档案时不会联网」，与新的行为不符，因此改写并请使用者重新确认。请求本身没有变化：只读、不带任何身份信息，关闭方式不变。
- **共享校准仓库支持更正与报错。**提交者名下的校准码被撤回后可以重新提交；同一账号再提交一份不同的校准码时，新的一份替换旧的一份（旧码没有其他提交者时标为已撤回）；已撤回或已被替换的校准码不会再次发布。公开仓库新增「报告校准有误」表单，报告由维护者核实后处理，不会按报告数量自动撤回。索引格式不变，已发布的版本照常读取。
- **交给使用者试用的构建改用先行版号 `X.Y.Z-beta.N`，不再占用正式发布号。**1.2.2、1.2.3、1.2.4 与 1.3.0 都只是本机测试包，却各自用掉了一个发布号；今后测试包一律带 `-beta.N` 后缀，只作为预发布分发，正式发布时才把后缀清空。更新检查据此判定：先行版比同号正式版旧、比更低的发布号新，正式版用户不会被引向测试包。
- **仓库改为 `main` 与 `dev` 两条长期分支。**`main` 只保留已发布并打过 tag 的提交，`dev` 是集成分支，功能分支与测试包都从这里切出；Pull Request 的目标分支相应改为 `dev`。流程见 `docs/release-checklist.md` 第 9 节。

### 修复
- **保存记录时按 Esc 不会再丢掉已经填好的内容。**「补录 / 修正记录」「核对校准」「导入校准码」三个窗口在点下保存、请求还没回来的这段时间里，各个按钮早就变灰了，唯独键盘这一路没有挡住：此时按 Esc 能把窗口关掉，而请求已经上路、收不回来。若随即为另一条记录重新打开窗口，先前那条的回应到达时会把眼前这个窗口直接关掉、刚填的内容一并丢失；若那条请求是失败的，上一条的错误提示还会显示在这张表单里。现在请求未完成时窗口关不掉，回应回来也会先核对它属不属于眼前这一次提交，不属于就不再生效。（此前的修改从不会写到别的记录上——请求送出时内容就已经是对的，出问题的只是界面这一层。）
- **总览页「通关 / 未通关」连点两下不会再多出一条重复的修订。**待复核的记录上这两个按钮此前在请求送出后仍可点击，手快点两下就会发出两条修正请求：第二条撞上修订号冲突后会自动用新的修订号再发一次，于是修订历史里多出一条与上一条内容完全相同、而用户并没有要求过的记录。现在按钮从请求送出起保持禁用，直到采集服务回应。
- **连着拨动两个捕获设置开关时，后一个不再被弹回旧值。**采集服务较忙时，第一次设置写入的回应可能迟迟不到；如果在这期间又拨动了另一个开关，第一条回应到达时会被当成「两次都写完了」，而两秒一次的状态轮询若恰好带回第二次写入生效前的旧快照，界面就会把刚拨的开关弹回去，直到第二条回应真正到达才纠正回来。现在只有全部写入都已回应，轮询快照才会覆盖界面上的设置。
- **记录较多时点「完整性校验」不再误报采集服务没有响应。**完整性校验要把整个数据库文件通读一遍，记录攒得多时需要好几秒，采集服务为此专门把它放到后台执行、不占用其他请求；界面这一侧却仍按普通请求的 8 秒计时，于是记录越多的人越容易一点这个按钮就得到「采集服务未在超时时间内响应」，而校验其实还在正常进行。现在界面会等到校验真正结束。同一时刻也只允许进行一次校验：重复点击会被告知正在校验，而不是把整个数据库再扫一遍。
- **卡死或异常退出的程序不会再一直占着采集服务的连接名额。**采集服务最多同时服务 8 个连接。此前一个连上来却再不说话的程序会把名额一直占住，占满 8 个之后记录器界面就再也连不上，而且这一过程没有任何提示。现在连续三分钟没有任何来往的连接会被关闭并记入日志。正常使用不受影响：界面每两秒就有一次状态查询，实时事件的订阅也有每五秒一次的心跳。
- **校准期间每两分钟一次的证据保存不再拖慢采集。**校准时软件每两分钟把观察到的证据写入磁盘一次，以免崩溃或断电时白白丢掉。这份证据可能有数千条，写一次需要一点时间；此前这段时间里，负责把报文逐条交给解析的那条线程被整个占住，而它前面的报文队列容量有限——报文密集时（例如换区加载的那一刻）队列就可能被填满、开始丢弃报文，恰好丢在校准最需要连续观察的时候。现在写盘交给后台完成，不再占用那条线程，界面上的校准状态刷新也不再等它。停止采集时仍会等这次保存写完，最多五秒。
- **证据不完整时，不再把某条报文认定为「匹配成功」公告。**校准判断一条报文是不是服务器的匹配公告时，会核对它在同一位置有没有出现过别的取值——这道检查专为防止一条报文身兼数职：1.2.1 修复过的那次，就是学到的匹配报文在雇员铃响时也会出现，结果凭空多出一条「匹配成功」，真正的那一局反而没记上。可用来核对的那张表有容量上限，一旦写满，某个位置「从没见过别的取值」可能只是因为它根本没被记下来；此前这种情况被当作「没有矛盾」直接放行。现在证据不完整就不作此认定，改由你申请了哪个随机任务来推断匹配，校准照常进行，只是不再凭一张写满的表下结论。
- **撤下一份被证实有误的共享校准时，不会误删刚刚换上的那一份。**同一个游戏版本只有一个共享校准文件。此前，正在使用的共享校准被本机流量证实有误而撤下时，若恰好另一份更准的共享校准刚刚核实通过、写好并启用，撤下那一步的删除可能落在写入之后，把刚换上的那份从磁盘上删掉，而软件仍以为它在用；下次启动时找不到这份档案，这个版本的校准状态被无端清零，只能重新下载、重新核实。现在两者依次进行，撤下前先确认文件上是不是已经换成了别的，是就不再删除。
- **「立即检查」连续点击时不再重复下载。**此前两次「立即检查」之间唯一的限制是「上一次还没结束」，上一次一结束——哪怕几十毫秒就以失败告终——下一次就能再完整下载一遍（索引加上每份候选校准码，最多约 27 个请求）。现在两次手动检查之间至少相隔五秒；这段时间内再点，界面会说明刚刚检查过、结果已在校准卡片上，既不报错，也不再说「已经在获取了」。「游戏更新后自动获取」原有的六小时间隔不受影响。
- **「立即检查更新」不会再提前返回检查之前的旧结果。**软件启动时的后台检查恰好开始的那一瞬间点「立即检查更新」，此前可能不等那次检查完成就返回「已检查」，界面上显示的仍是检查之前的状态。现在会等到那次检查真正结束。
- **捕获诊断页的「最近有效事件」现在会显示服务器播报的匹配通知。**使用 2026.09.15 之后国服客户端档案的玩家，此前在这里只看得到一个时间、看不到事件名称，无法直观确认解析链路已经认出了匹配成功播报。
- **「未检测到 Npcap」提示条在浅色艾欧泽亚风格下的颜色与页面其他橙色一致。**此前它固定使用深色风格的橙色。
- **「手动修正」不会再把修改存到另一条记录上。**在修正对话框里点选结果、副本或职业时，这一次点击会同时落到对话框正下方的历史记录行上，当前选中的记录因此被悄悄换成另一条，随后的保存就写到了那条记录上：想补职业的那条记录毫无变化，再点一次保存又提示「未做任何修改」，而对话框下方恰好压着的那条记录却被写上了职业。现在对话框内的点击不再传到它下面的页面，所有对话框一并如此；保存时还会核对对话框打开时的那条记录是否仍是当前选中的记录，不一致就拒绝保存并提示重新打开，绝不写到别的记录上。此前被误写的记录可在其「修正历史」中逐条撤销。
- **修正对话框里的日期与时间不再显示上一条记录的内容。**在时间框里点过一次之后，同一次运行中再为别的记录打开对话框，时间框仍显示之前那条记录的时间，重启软件才恢复；实际提交的始终是当前记录的时间，只是显示有误。现在每次打开都显示当前记录的时间。
- **修正记录时不再附带一条空备注的改动。**原本没有备注的记录每修正一次，修订里都会多出一条「备注：空 → 空」。
- **更准的共享校准核实通过后，当场就换上，不必等下次登录。**软件正用着一份按排本推断的校准时，如果公开列表里出现了一份认得出服务器匹配报文的校准，会在后台核实并换上它，换上之后匹配弹窗才有「匹配成功」的语音。此前这一步几乎总是落空：一局刚结束的那一刻软件误以为这一局还没收尾，换用被推迟到下一次登录，当晚剩下的几局仍然没有匹配播报。现在一局结束后立即换用；如果核实通过时正在副本里，或者已经排上了本，就等这一局打完再换，这一局照常记录，之前的记录也不会被标记待复核。
- **首次用上共享校准时不再迟报「匹配成功」。**共享校准往往在进入副本的那一刻才核实通过，此前软件会把几秒前的匹配弹窗和进本连着补报一遍，于是进本时才听到一声迟到的「匹配成功」。现在补记的匹配只写入记录、不再播报，进本照常播报，之后的匹配弹窗照常即时播报。
- **进行中的记录不再显示「检测置信：中」。**置信度在记录结束时才评定，进行中的记录此前显示的只是初始值，容易被读成已有结论，现改为显示「结束时评定」。另外，「高」此前只认副本的内容编号，由区域编号认出副本的记录无法达到；现在两者都算，但仍要求结局来自结算报文。当前国服档案不含结算报文，记录结束后仍为「低」并等待确认，这一点没有变化。
- **共享校准被撤回、或者出现更准的一份时，正在用它的机器现在也能知道。**以前一旦用上了某份校准，软件就不再去仓库看一眼：别人发现那份校准有错并把它撤回了，或者后来有人分享了更准的一份，这台机器都无从得知。现在，当正在使用的档案来自共享校准，或者它按「你申请了哪个随机任务」来判定匹配时，软件会按原来的频率（同一版本 6 小时最多一次）再读一次索引：在用的校准码被撤回就换下它，它生成的记录标记待复核，并改用同一份索引里的次优码，没有则回到本机校准；出现认服务器报文的更准的码就在后台核实，通过之后自动换用——旧的那份只是不够准，不算出错，它生成的记录不会被标记，它的校准码也不会被拒绝。换下与换用都等手头这一把记录结束之后才进行，进行中的记录不会被截断。随包档案、以及认服务器报文的本机档案在用时，仍然一次请求都不发。读取的请求与以前逐字节相同，同样不上传任何内容；详见 docs/privacy-boundary.md §8.2。
- **已完成的本机校准会自己补上职业。**职业报文的判据修好之前写出的本机档案，如果它认得出服务器的匹配报文，校准就已经结束了，于是这一版游戏的每一条记录都永远记成「职业未知」，且没有任何办法补救（1.2.3 的补职业只适用于按排本推断的档案）。现在这类档案旁边会静静地继续校准（不显示卡片、不打扰，记录照常生成），直到某一晚的观察同时认出了职业、且其余每一条报文都与当前档案完全一致，才请你核对一次，之后的记录就带上职业。匹配报文与当前档案不一致的观察永远不会在这里被提出来，以免静悄悄换掉一条今天工作正常的匹配报文。
- **征求同意时也能导入校准码。**自动下载到的按排本推断的共享校准核实通过后，卡片会停在「同意一次」上，而这个状态下「导入校准码」按钮被藏了起来：同意就用上了这份较弱的校准，拒绝则这个版本不再使用任何共享校准，手里拿着朋友发来的更好的校准码的人反而无路可走。采集服务从未在这个阶段拒绝过导入，现在界面也不再藏它：橙框里多一个「导入校准码」，说明文字也直接点明这条退路。导入的、能读出服务器匹配报文的校准码核实通过后，优先于正在征求同意的那份。
- **有人取消后再次匹配时也播报「匹配成功」。**第一次匹配播报之后，若有人取消、任务搜索器重新组队，第二次弹窗没有语音：再次匹配仍属同一条记录、同一个状态，采集服务因此不发任何事件；超过匹配窗口才再次匹配时，旧记录收尾与新记录开启发生在同一步，前后状态相同，同样不发事件。现在与上一次弹窗相隔 10 秒及以上的再次匹配会再发一次状态事件（可选字段 `match_offer`，见 `contracts/CHANGELOG.md`），桌面端据此再播报一次；同一次匹配连发的数条报文与断线重连后的重放仍只播报一次。
- **坦克职业在所有界面风格下都显示为蓝色。**职业统计等处的坦克颜色此前取自界面的强调色，只有经典风格的强调色是蓝色，艾欧泽亚为金色、Harendotes 为橙色。现在固定取各风格调色板中的蓝色。

### 已知限制
- 首次使用、尚未见过游戏进程时没有可记住的目录，仍需启动一次游戏；游戏目录被移动或卸载后同理。
- 游戏未运行时读到的是**已安装**的版本。补丁日启动器完成更新之前，磁盘上仍是旧版本；游戏启动后以运行中的客户端为准，档案随之重新匹配。

## [1.3.1] - 2026-09-19

1.2.2 至 1.3.0 仅作为本机测试包构建，没有单独发布；其内容见以下各节，随本版本一并发布。

### 变更
- **确认结局不再标记为「已修正」。**随包与本机校准的档案都无法观察副本是否通关，每条自动记录都以待复核收尾，由用户在「本次导随结果」中回答；此前这一回答与任何人工改动一样被记为「更正」，历史记录因此把所有自动记录都标为「已修正」。现在只有改动了软件已记下的内容（副本、职业、时间，或已有定论的结局）才算更正并标「已修正」；回答待复核记录的结局、补上原本为空的职业或副本、编辑备注不算，这类记录标「已确认」。修订记录照常写入，审计链不变。历史记录的「已修正」筛选随之只列出真正被更正的记录。
- 既有数据在采集服务启动时按各自的修订链重新判定一次 `manually_corrected`：不产生修订，不改动更新时间，不提升数据库版本，可重复执行（`docs/data-model.md`）。

## [1.3.0] - 2026-09-19

### 新增
- **按出现时机认出「匹配成功」报文，弹窗出现时即可播报。**2026.09.15 国服客户端的匹配通知在任何字节位置都不携带所排的轮盘编号，按数值寻找的三种方式均无结果，档案只能按排本申请推断匹配，语音因此要到进本时才播报。现在本机校准另外记录每种服务器报文出现的时机：只在排本期间（或被队长带进已知副本之前）出现、每一次自己申请的进本之前都出现、其后通常跟着副本、不属于任何换区加载、每次排队不超过 8 条、且比读条至少提前 3 秒的那一种，认定为匹配通知，写入档案的可选消息 `MATCH_ANNOUNCED`。所排的随机任务仍由排本申请确定，通知只提供匹配的时刻。认定需要至少两次、涉及两个不同随机任务的自己申请的进本；核对时间线上每次进本给出一行「匹配弹窗：随机任务名（按出现时机认出，读条前约 N 秒）」，标为「错」只否决这条报文，不影响已在使用的记录方式。详见 `docs/protocol-profile-format.md` §11.5 与 `docs/state-machine.md` §3.12。
- 通知只增不减：通知漏抓时仍在进本时生成记录；通知过早或认错时，120 秒窗口之后进入已知副本照常记录，非副本换区只收尾该次匹配并保留排本申请。同一次匹配的重复通知最多记入 16 次。
- 脱敏诊断报告的 `calibration.evidence` 新增 `timed_candidates` 与 `timing_overflow`。IPC 契约附加变更：`last_valid_event_kind` 的枚举新增 `MATCH_ANNOUNCED`，不新增消息类型（见 `contracts/CHANGELOG.md`）。

### 已知限制
- `MATCH_ANNOUNCED` 不进入校准码（格式 v1 不变）：带有它的档案分享出去仍是按排本申请推断的档案，接收方在本机自行识别。
- 用户作为队员参加的指导者任务仍无法记录：队员的客户端不发出排本申请，无从得知所排的随机任务。

### 修复
- **进行中的记录不再参与统计。**副本进行期间，总览把正在进行的这一次计入尝试次数与「未知结果」，通关率在整场副本期间被拉低。现在自动记录在结束之前不参与任何统计；崩溃恢复后交由用户复核的未结束记录照常计入（`docs/statistics-definitions.md` §0）。
- **历史记录中进行中的记录显示「进行中」**，不再显示为「未知」；记录详情同步。

## [1.2.4] - 2026-09-19

### 修复
- **核对时间线过长时，对话框底部的按钮不再被挤出窗口。**时间线包含当次观察到的全部登录、换区与排本，游玩一段时间后可能高于窗口；此前对话框随之增高，「以后再说」与「核对并启用」落在窗口下沿之外，校准既无法确认也无法推迟。现在对话框高度不超过窗口，时间线在其中滚动，两个按钮始终可见。

## [1.2.3] - 2026-09-19

### 修复
- **2026.09.15 国服客户端上职业不再一律记为「未知」。**该版本同时用两条报文通告职业：出现在相同的换区中，每次读到的职业编号都相同。本机校准此前要求满足条件的职业报文恰好只有一条，两条并列时不声明，因此该版本上校准出的档案都不含职业报文。现在多个候选在每次换区中读数都一致时，视为同一个答案，取 opcode 最小者，以便同一版本的所有机器写出相同的档案与校准码；读数不一致时仍不声明（`docs/protocol-profile-format.md` §11.3）。
- **已生效的按排本推断的档案可以补上职业报文。**此前档案生效后，后台得出的「同样按排本推断」的结论不会再次提请核对，缺少职业报文的档案因此无法补全。现在结论中含有生效档案所缺的职业报文时，会再请用户核对一次；确认后档案重写，并在当前抓包会话内立即换用（有记录正在进行时，延后到下一次会话）。

## [1.2.2] - 2026-09-19

### 修复
- **校准卡片不再在已经开始记录时声称「不会生成记录」。**按排本申请推断匹配的档案生效后，软件仍在后台寻找服务器发出的匹配报文。此前该后台查找受阻时，卡片标题显示「本机校准无法继续，当前不会生成记录」；软件重启、执行「清空进度并重新观察」或游戏尚未启动时，标题又退回「正在重新校准……期间不会生成记录」，均与下方「已经可以正常记录导随了」的说明相矛盾。现在只要有档案在记录，状态保持为观察中，并始终携带该档案的编号，桌面端在等待游戏启动时也按「已在记录」显示。
- **后台查找受阻时如实说明。**此前卡片仍提示「打两把不同的随机任务就够了」，而受阻的证据会跨重启保留，继续游戏不会有进展。现在说明记录不受影响、这次积累的观察已经失效，以及重新开始的步骤：先启动本软件，再启动游戏并登录，然后执行「清空进度并重新观察」。
- **说明组队排本的限制。**组队排本时只有队长的客户端发出排本申请，因此按排本申请推断匹配的档案无法记录用户作为队员参加的任务，这些任务也不能用于本机校准。卡片说明中已写明。
- **核对时间线：同一次匹配连发的报文合并为一行。**部分版本会为一次匹配连续发送数条匹配报文（2026.09.15 国服客户端上一次匹配为 3–4 条），此前每条各占一行并各需确认一次。现在相同随机任务、间隔不超过 60 秒的报文合并为一行，并注明连发次数。
- **核对时间线：当晚其他副本显示名称。**此前只有校准所依据的那一次进本显示为「进入副本：名称」，其余已识别的副本一律显示为「换区」，容易被理解为副本名缺失。现在显示为「换区：进入了 名称」，该行无需确认。

### 新增
- 脱敏诊断报告的 `calibration.evidence` 新增 `job_shapes`：职业报文形状上每个 opcode 获得多少个换区簇佐证、被多少个簇否决，以及读到的职业编号（与记录中保存的职业编号相同）。`progress.job_seen` 为 false 时，该行用于区分「没有候选」「两个候选并列」与「候选被否决」（`docs/capture-diagnostics.md`）。报告版本仍为 2。

## [1.2.1] - 2026-09-18

### 修复
- **本机校准不再把雇员列表认作「匹配成功」。**2026.09.15 国服客户端上，排本期间打开雇员铃会收到一串长度 80 的报文，第 16 字节依次为栏位序号 0–9，其中总有一行与所排的轮盘编号相同；校准据此把该报文（`0x02F5`）写进了已启用的本机档案。结果是：排指导者任务时每打开一次雇员铃就播报一次「匹配成功」并生成一条「进本前取消」的记录，而真正的匹配、进本与结束全部没有记录。现在「匹配成功」按独立报文识别时增加一致性检查：排本期间同一形状的报文在该位置携带其他取值达到 2 次，即不予采信（`docs/protocol-profile-format.md` §11.3）。
- **已启用的错误本机档案会自动撤下。**本机档案的匹配报文为服务器发出且不含选择器时，记录期间持续检查：1 秒内读到 3 个及以上不同的轮盘编号，即判定所认报文不是匹配通知。此时该档案立即停止使用，文件改名为 `<profile_id>.json.contradicted` 保留，由它生成的记录以系统修订标记待复核（用户已亲自修改过结果或复核状态的记录保持原样），本机校准重新进入观察（§11.4）。受影响的用户升级后无需手动删除任何文件：排本期间打开一次雇员铃即触发撤下，之后再完成两个不同的随机任务即可重新校准；2026.09.15 版本上真正的匹配报文尚待重新识别。
- 公开仓库中 2026.09.15 版本的那份共享校准（`f1efd32d9c83`，内容即上述错误报文）已撤回。已绑定它的客户端由既有的核实机制在两个抓包完整的会话后撤下。

## [1.2.0] - 2026-09-18

### 新增
- **第三种界面风格「Harendotes」。**设置 → 通用 → 界面风格新增一项。深色「夜色狼身」：海军蓝黑底、橙焰主色、古金仅作点缀；浅色「白胸冷光」：纯白面板、冷调蓝灰背景、浅色顶栏。标题用冷白宋体；面板与侧栏状态面板的外框为自左上到右下、由橙焰经古金淡出的对角渐变，标题栏底边为焰→金细线；成就进度环沿弧线由黄焰渐变到末端的深橙焰。与艾欧泽亚共用布局，`--mock-ui-style harendotes` 可在截图模式固定该风格。
- **「检查更新」按钮。**设置页"通用 → 更新"与"关于"页各有一个：点击立即检查一次（不受每日节流），结果以一句提示说明；有新版本时按钮变为「打开下载页」。开关「检查新版本并提示」关闭时按钮不可用，不发出任何请求。IPC 新增一条消息 `CheckUpdateNow`（异步应答），业务消息增至 49 个（见 `contracts/CHANGELOG.md`）。

### 变更
- **艾欧泽亚风格改为游戏自身的窗口配色。**深色：暖灰石板面板、浅色文字、深灰按钮与页签；浅色：羊皮纸面板、深棕文字、深棕按钮与页签。金色只保留给选中标记、侧栏标题、进度环与柱条，不再作为面板边线、标题与按钮的主色。经典风格不变。设置页界面风格的副标题改为「艾欧泽亚：游戏窗口配色」。
- **每次启动检查一次新版本。**此前上次检查的时间持久化保存、24 小时内重启也不再检查，用户重启软件时看不到任何检查动作。现在每个进程启动后第一次状态轮询就检查一次（上次检查距今不足 1 小时则不检查，避免反复重启变成反复请求），之后仍是每 24 小时最多一次。隐私边界 §8.4 与 README 同步。

## [1.1.0] - 2026-09-18

### 变更
- **共享校准登录时即可启用，第一把随机任务就有记录与播报。**此前下载来的共享校准要等第一次排本、第一次进本都核实通过才开始记录，游戏更新后的第一把只能事后补记、没有语音。现在核实分两步：登录时换区报文按结构核实通过即启用；排本与进本报文在记录中继续核实，两个抓包完整的会话都对不上时自动撤下并改回本机校准，这期间由它生成的记录以系统修订标记待复核并给出原因。
- **导入校准码按来源区分。**导入时只比对本机此前下载到的索引，不联网：索引列出的码按下载的码对待；索引已撤回的码拒绝导入并说明；任何索引都不认识的码（例如群里转发的）仍须排本与进本全部核实通过才启用，在此之前不产生记录。之后取到的新索引若列出了这份码，自动改按下载的码对待。导入结果的提示语随之区分两种情况。
- **公开仓库索引新增 `conflicting` 标记**：同一版本、同一模板、同一匹配方式下出现互不相同的码时，双方都被标记，本软件把它们排到核实顺序的末尾。旧版本忽略该字段。
- IPC 契约附加变更：`calibration.shared` 新增可选 `audit_pending`，候选新增可选 `provenance`（`PUBLISHED` / `IMPORTED`）与 `audit_pending`；`ImportCalibrationCode` 应答新增可选 `provenance`，`reason` 新增 `REVOKED`。不新增消息类型（见 `contracts/CHANGELOG.md`）。隐私边界 §8.2 与 README 同步。

## [1.0.0] - 2026-09-17

### 新增
- **检查新版本并提示（默认开启，可关闭）。**采集服务每 24 小时最多检查一次（另附 0–2 小时的随机抖动），读取本项目发布页上 `BUILD-METADATA.json` 中的版本号，与当前版本比较。发现更高的版本时，总览页出现横幅「有新版本 x.y.z」，其上提供「打开下载页」与「忽略此版本」；「设置 → 关于」在版本号旁同样显示「有新版本」。**本软件不下载安装包，不执行取回的任何内容，也从不替换自身的任何文件**，下载与安装仍由用户在浏览器中自行完成。请求只有一个 `GET`，没有请求正文，不携带查询串、Cookie、安装编号或账号，请求头只有不带版本号的 `User-Agent: MentorRecorder`；仓库尚未公开而返回"未找到"、断网、被限流或内容不合法时一律静默降级，界面上不出现任何提示。检查只在桌面端连接并轮询期间顺带发生，没有桌面端连接时不会发出。可在「设置 → 通用 → 更新」中关闭「检查新版本并提示」；如需完全禁用，将系统环境变量 `MR_DISABLE_UPDATE_CHECK` 设为 `1` 后重启本软件。完整说明见 `docs/privacy-boundary.md` §8.4。

### 变更
- **隐私边界新增一类出站请求：更新检查。**出站请求现共三类，均由采集服务发出：共享校准下载（默认开启）、在线语音合成（默认关闭）与更新检查（默认开启，可关闭）。`docs/privacy-boundary.md` §1、§2、§8、§8.4、§9，以及 README、CONTRIBUTING 与首次运行说明均已同步。
- **首次运行说明会再次出现一次。**说明页的联网一节由两类改为三类，新增「联网三：检查新版本」（默认开启，可关），其版本号随之提升，因此每一位既有用户在升级后都会再看到一次该页并重新确认。更新检查默认开启，且没有单独的确认框，该说明页是让用户知情并了解关闭方式的位置。
- IPC 契约附加变更：`CollectorStatus` 新增可选的 `update` 对象，`GetCaptureSettings` / `UpdateCaptureSettings` 新增 `update_check_enabled`。不新增消息类型，业务消息仍为 48 个（见 `contracts/CHANGELOG.md`）。脱敏诊断报告新增 `boundary.outbound.update_check`，其中不含主机名与地址，报告版本仍为 2。
- 静态边界检查：`NET-006` 精确放行更新检查客户端一个文件，例外增至三个；`NET-007` 新增 `github-content-hosts` 变体，`githubusercontent` 可以出现在更新检查客户端与既有的共享校准几处，此外任何位置都报错，`jsdelivr` 仍只限于共享校准一侧。
- `scripts/verify.ps1`、`scripts/test.ps1`、`scripts/package.ps1` 一并设置 `MR_DISABLE_UPDATE_CHECK=1`，因此验证、测试与打包期间不会联网。

## [0.9.1] - 2026-09-17

### 新增
- **安装程序检查最低系统要求。**低于 Windows 10 版本 1809（内部版本 17763）的系统被拒绝安装，并给出所需版本。ARM 处理器上的 Windows 10 同样拒绝；Windows 11 on ARM 提示抓包未经验证后继续安装。缺少 Media Foundation 的 Windows “N” 版本提示语音播报需要媒体功能包后继续安装。README 的系统要求同步更新。
- 桌面端 C++ 新增静态分析脚本 `scripts/static-analysis.ps1`（clang-tidy 与 cppcheck，检查项定义在 `.clang-tidy`），并按其结果消除若干告警：`AppController` 标记为 `final`（其构造函数会经虚槽调用 `refreshStatus`）、IPC 帧长度先拓宽再相加、`actionEnabled` 去除重复判断、模拟后端合并两个相同分支、测试中的空指针断言改为逐个断言，另有若干处 `explicit`、指向 const 的指针参数与构造函数初始化列表的修正。

## [0.9.0] - 2026-09-17

### 新增
- **按钮、导航与状态板配备图标。**侧栏六个页面的序号替换为各自的线条图标，颜色随当前页高亮。左下角状态板按原型收为三行（FF14 / Npcap / 捕获），行首圆点替换为按状态着色的图标。历史记录页的「导出 CSV」「导出 JSON」「新增记录」「清除」「上一页」「下一页」以及标题栏的深浅色切换均配有线条图标，颜色跟随按钮文字。图标来自 Lucide（ISC 许可证，其中几个 Feather 派生图标同时受 MIT 约束，见 `THIRD_PARTY_NOTICES.md`），任何按钮指定 `iconName` 一项属性便可使用。
- **新增 / 修正向导的步骤切换带动画。**新的一步淡入并按方向滑入，下一步自右侧进入，上一步自左侧进入。系统关闭动画时不播放该效果。
- **向导中的日期与时间支持快捷输入。**日期与历史记录页一致，通过日历选择。时间仅需输入数字，2130 自动写成 21:30，930 写成 09:30；也可点击右侧的时钟图标用滚轮选择时分，或一键填入「现在」。
- **历史记录页的日期改为日历选择。**起始与结束日期显示为「年/月/日」，点击右侧的日历图标弹出月历选择日期，同时仍支持直接输入 yyyy-MM-dd。

### 变更
- **窗口背景按原型绘制两层径向光晕**，顶部为冷色、右下为暖色，深浅两套配色，两种界面风格一致。此前经典风格接近纯色。标题栏的深浅色切换按钮改为反色：浅色页面上为深色按钮，深色页面上为浅色按钮。
- 历史记录页的筛选栏按原型收为一行：移除「待复核」开关，从总览「去复核」下钻进入时改以可移除的标签呈现；「清除」改为纯文字。
- 历史记录、副本、职业等处的副本类型图标与职业图标不再附加本软件绘制的底板，仅在缺少图标、以文字代替时保留。图标本身不变，包括职业图标自带的职能边框。
- 移除历史记录页底部"每页 10 条 · 分页加载，不一次载入全部"一类说明实现方式的文字。
- 移除未被任何页面使用的 `DataTable` 组件。历史记录表另有独立实现。
- IPC 契约中 Azure 语音服务的描述不再写出主机名。主机名只出现在在线语音客户端这一个文件里，与 `docs/privacy-boundary.md` §8.3 的说法一致。
- 完整性校验补充损坏文件、锁忙、超长问题描述与"持锁时不等待"的测试。
- 集成测试中契约 schema 的评估改为串行执行。JsonSchema.Net 在首次评估时才完成 schema 构建，多个测试类并发评估同一实例时会偶发地把不合法的请求判定为合法，约四次出现一次随机失败。该问题与被测代码无关。

### 修复
- 修复下拉框弹层中每个选项多出一行同名小字、长选项被截断为"讨伐歼…"的问题。没有分组的下拉框不再附加分组标题，弹层按最长选项自动加宽。
- 历史记录页的「职业」筛选只列出战斗职业，不再包含生产采集职业与基础职业。
- **数据页「完整性校验」不再阻塞实时记录。**0.8.0 中该校验与抓包写入争用同一把锁，数据库较大时全表扫描需要数秒，副本进行中触发可能导致结算事件排队甚至丢失。现在校验在采集服务自身的一条只读连接上执行，并与在线语音一样异步应答，期间抓包与其他请求照常处理。契约见 `contracts/CHANGELOG.md`。
- 在播报页选择同一在线服务的另一音色、而采集服务正忙未能记录时，此前需要切换到其他音色再切回才会重试。现在下一次读取设置或再次操作时会自动重试一次。被明确拒绝的音色仍然只尝试一次。

## [0.8.0] - 2026-09-17

### 新增
- **在线语音（默认关闭）。**「设置 → 播报」的语音引擎除本机已安装的系统语音外，新增微软 Azure 语音（晓晓、云希等中文自然语音）与任何 OpenAI 兼容的语音接口。选中后填写区域或地址与密钥，点击「测试」试听。首次选择在线语音时会先行说明三点：出站内容只有当前要播报的这一句话，即模板填充后的整句，含副本名与进度数字，发往用户选定的服务；密钥只保存在本机，由 Windows 加密，不写入日志与诊断报告；改回本机语音后不再发出任何请求。请求由后台的采集服务发出，桌面端本身仍然不联网。在线语音出错或超时时，该句自动改用本机语音播报，并提示一次原因。如需完全禁用，将系统环境变量 `MR_DISABLE_ONLINE_SPEECH` 设为 `1` 后重启本软件。完整说明见 `docs/privacy-boundary.md` §8.3。
- **本机语音支持选择音色。**语音引擎下拉框列出本机已安装的系统语音，中文语音排在前面，所选音色会被记住。
- **新增与修正记录改为三步向导。**第一步选择结果，共六张卡片，各带一句说明，并预告保存后成就进度是否 +1；第二步选择副本与职业；第三步填写时间与原因。副本可按 4 人本 / 8 人本 / 24 人本、等级段、普通 / 极 / 零式筛选，也可按名称、等级或版本搜索，列表上方另有“最近打过”的副本。职业按坦克、治疗、近战、远程物理、魔法分组。字段校验失败时直接跳回该字段所在的步骤。
- 数据页新增「完整性校验」按钮，可随时请求采集服务检查数据库，结果显示在按钮旁边。
- 捕获诊断的“最近有效事件”补充事件类型（如“副本结算”“匹配成功”），不再只显示时间。

### 变更
- **界面改版（设计稿 2026-09）。**
  - “经典”风格采用新的配色与字号，数字改用等宽字体，**并成为默认风格**。升级后，设置中未保存过风格的用户会看到经典风格，其中包括一直使用原默认风格“艾欧泽亚”、从未切换过的用户。如需继续使用艾欧泽亚，在“设置 → 通用 → 外观”中切回，此后一直保留。艾欧泽亚风格本身的外观不变。
  - 设置页改为左侧分栏：通用（启动与托盘、记录、校准、外观）、播报、成就、数据、关于。原有的每一项均予保留。
  - 捕获诊断页改为三块：“链路”按 FF14 进程 → Npcap → 适配器 → 协议档案排列，便于定位未就绪的环节；“解析”显示成功率、消息速率、失败与去重、最近有效事件、最近失败；“工具”集中维护者功能。普通玩家的标题栏显示“自动监听中 / 等待游戏启动”等状态，开始 / 停止按钮仍只在维护者模式中提供，维护者工具统一置于页面最下方。
  - 文案移除术语：“心得”改为“笔记”，“baseline”改为“基数”，“rev”改为“修订”，“Collector”改为“采集器”。当前导随的状态与检测置信改为中文显示，标题栏只保留“导随记录器”，修正与撤销后的提示不再附带审计编号。
  - 历史记录表移除“匹配”一列，匹配时间仍保留在详情中，表头与内容重新对齐。
  - 动效：按钮按下、导航悬停、分段选择、开关按下均有轻微反馈。经典风格下，切换页面时内容自右侧滑入、卡片依次弹出、历史记录逐行浮现；完成趋势在“日 / 周 / 月”之间切换时，柱子合并或拆分到新的位置；切换深浅色时自点击处圆形展开。在 Windows 中关闭了“显示动画”的电脑上，上述动效全部停用。
- **隐私边界新增一类出站请求：在线语音合成。**出站请求现共两类，均由采集服务发出：共享校准下载（默认开启）与在线语音合成（默认关闭，仅在用户选定服务并填写密钥后才会发出）。首次运行说明、README、`docs/privacy-boundary.md` §2、§8、§8.3、§9 已同步。在线语音默认关闭且启用时另有确认，因此首次运行说明不会因本次变更再次弹出。
- 副本数据补充每个副本的人数，取自 xivapi 的队伍构成字段，按数据文件记录的游戏版本固定，用于向导中的筛选。其余字段与行序不变。
- IPC 契约附加变更：新增 `GetSpeechSettings`、`UpdateSpeechSettings`、`SynthesizeSpeech`、`CheckDatabaseIntegrity`，`CaptureStatus` 新增 `last_valid_event_kind`，新增七个 `ERR_SPEECH_*` 错误码（见 `contracts/CHANGELOG.md`）。
- 静态边界检查：`NET-006` 精确放行在线语音客户端一个文件；`NET-007` 按客户端分别限定主机名，两个客户端不得出现对方的主机。
- 安装包新增 Qt Multimedia（LGPL-3.0）与 IBM Plex Mono 字体（SIL OFL 1.1），只包含 Windows 自带的媒体后端，不包含 FFmpeg。

### 修复
- 修复设置页“UI 缩放”一行比卡片宽 12 像素、数值溢出卡片边缘的问题。
- 修复使用 `--mock-ui-style` 截图时“界面风格”开关显示保存的设置而非当前画面风格的问题。
- **重启本软件后仍可使用「分享给其他玩家」。**0.7.12 中该按钮只存在于校准卡片上，而校准完成并重启后该卡片不再出现，最应分享校准的用户反而失去了入口。现在只要当前使用的档案由本机校准得出，捕获诊断页始终可见的「协议档案」卡上即提供「分享给其他玩家」，行为与此前一致：用系统浏览器打开 GitHub 上预填的分享页面，并把校准码复制到剪贴板，是否提交仍由用户在浏览器中决定。当前使用其他玩家分享的校准或随软件附带的档案时，该按钮不出现。校准卡片仍在时按钮保留在卡片上，同一屏内不会出现两个。「导入校准码」仍位于校准卡片上，因为采集服务只在校准进行中接受导入。

## [0.7.12] - 2026-09-16

### 新增
- **桌面端的共享校准界面。**捕获诊断页的校准卡片新增“共享校准”一节，逐一说明各种状态：正在获取、已找到待核实、已使用其他玩家分享的校准（本机已核实）、与本机流量不符已改为本机校准、获取失败（网络不通）、该版本尚无人分享、已按用户选择改为本机校准。卡片提供「立即检查」、「导入校准码」与「不用共享的，我自己校准」三项操作。「导入校准码」用于无法连接 GitHub 时粘贴他人提供的校准码，导入过程不联网，同样先在本机核实；「不用共享的，我自己校准」需要确认一次。按排本推断匹配的共享校准核实通过后，卡片会说明这种记录方式的代价，用户同意后才开始记录。
- **分享给其他玩家。**本机校准完成并正在用于记录时，卡片上出现「分享给其他玩家」。该操作用系统浏览器打开 GitHub 上预填的分享页面，同时把校准码复制到剪贴板。是否提交由用户在浏览器中决定，本软件不上传任何内容。校准码超出网页地址长度限制时只复制，不打开浏览器。
- 设置页“通用”新增开关「游戏更新后获取其他玩家的共享校准」，默认开启，并写明这是本软件唯一的联网请求，请求中不含任何可识别用户的信息。

### 变更
- **存放共享校准的公开仓库已建立**：[MentorRecorder-Calibrations](https://github.com/Harendotes-Tang/MentorRecorder-Calibrations)。用户点击「分享给其他玩家」后在浏览器中提交，经仓库的自动流程校验通过后发布，其他玩家的软件方可下载。0.7.11 发布时该仓库尚不存在，那一版只会得到“未找到”。提交需要 GitHub 账号注册满 30 天，每个账号每个客户端版本一份。仓库中的脚本与模板采用 GPL-3.0-or-later，用户提交的校准码按 CC0-1.0 公开，提交表单中已写明提交即表示同意。
- 首次运行说明新增“唯一的联网：游戏更新后获取共享校准”一条，并删去已不成立的“没有任何出站网络请求”。说明内容发生变化，已确认过的老用户会再次看到该页。说明页正文改为可滚动，小窗口下“我已了解”按钮不再被挤出窗口。
- 共享校准的档案正在用于记录，且尚未完整记录一次进出本、校准仍在后台核对时，卡片标题显示“已使用其他玩家分享的校准”，不再显示“正在重新校准，期间不会生成记录”。侧栏协议标签显示“共享校准”。

## [0.7.11] - 2026-09-16

### 新增
- **共享校准（采集服务一侧）。**游戏更新后本机没有可用档案时，采集服务从一个固定的公开 GitHub 仓库只读下载其他玩家分享的校准码。下载后先在本机流量中按结构核实，登录时的换区与第一次排本即为足够的证据。核实通过后才写出档案，并在当前抓包会话中直接开始记录，无需重新登录。核实完成前不产生任何记录、语音或实时事件。下载失败或核实不通过时照常进行本机校准，已积累的证据不受影响。在两个抓包健康的会话中均与本机流量不符的校准码会被撤下，此后不再尝试；「重新观察」会清除这些判定。设计与评审记录见 `docs/plans/shared-calibration.md`。

### 变更
- **隐私边界变更：本软件首次具备主动联网行为。**唯一的出站请求是上述共享校准下载：只读 `GET`，不带任何标识，从不上传，仅由采集服务发起，且仅在当前版本没有可用档案时发出。桌面端仍然不发起任何网络请求。GitHub 与 jsDelivr 可以看到请求来自哪个 IP 地址。完整说明与自行验证方法见 `docs/privacy-boundary.md` §8.2。
- **本版本的已知限制。**桌面端尚无共享校准界面，设置开关、「立即检查」、分享、导入与“按排本推断”的同意提示均在后续版本提供。存放共享校准的公开仓库尚未建立，因此本版本的下载只会得到“没有找到”，不会使用任何共享数据，需要用户同意的共享校准也不会启用。已有可用档案时（包括本机校准得出的档案）不发出请求。如需立即彻底关闭该下载，将系统环境变量 `MR_DISABLE_SHARED_FETCH` 设为 `1` 后重启本软件。
- 诊断报告版本升为 2：删去始终写死为 0 的“出站连接数”，改为如实报告共享校准开关、总开关，以及本进程上一次实际发出获取的时间与结果。报告新增 `calibration.shared`，读法见 `docs/capture-diagnostics.md` §9.6。
- IPC 契约附加变更：`profile_origin` 新增 `SHARED_CALIBRATION`，校准状态新增可选的 `shared`，采集设置新增 `shared_calibration_enabled`，另有五个共享校准请求与错误码 `ERR_SHARE_CODE_UNAVAILABLE`（见 `contracts/CHANGELOG.md`）。
- 静态边界检查：`NET-006` 只放行共享校准下载客户端这一个文件，新增 `NET-007` 限制下载源主机名可以出现的位置。检查器支持按精确路径放行。

### 修复
- **记录中的职业不再始终为“未知”。**本机校准此前要求职业报文在每一次换区（包括登录时的大厅握手）中都出现，且整晚只要出现一条越界读数就永久淘汰该报文。本版本游戏的登录握手不携带职业报文，导致每条记录的职业都写为未知，界面上亦无任何提示。现在只要求进本、出本两次换区都携带该报文且取值稳定，并在全部换区（含进本、出本）中至少半数出现。越界读数只在同一次换区内计为矛盾，换区之外的仅计数。
- 校准确认或档案在会话中热切换后，第一条记录的职业不再需要等待下一次换区：观察期间最后一次读到的职业直接沿用。
- 校准卡片新增“职业”一行。尚未识别时明确显示“先记为未知”，以说明记录中缺少职业的原因。
- 修复经典界面风格下鼠标划过侧栏导航时，经过的每一项闪现深灰色底块、快速移动时两项同时发灰的问题，该现象在浅色主题下最为明显。悬停底色此前自“透明的黑色”渐变，中间态为半透明的灰；现在自同一颜色的全透明渐变，只改变透明度。幽灵按钮的悬停，以及从艾欧泽亚切换到经典时主按钮（如设置页的“保存”）闪现黑色，一并修正。

## [0.7.10] - 2026-09-14

### 修复
- 当前导随在未识别到职业时隐藏职业栏；结束后的结果与心得弹窗提供职业补录，随确认一起保存，失败后保留选择。
- 历史修正保留仅靠区域识别或不在当前副本表中的副本信息，修正结果不再把名称覆盖为“未知副本”；手动匹配副本编号时同步填写副本名称和种类。
- 本机校准的排队推断模式改为确认进本后才创建记录，修复取消排队后换区仍挂着“未知副本”的问题；正常排队期间传送后仍可继续记录进本。
- 匹配语音依据该次事件的实际来源播报；仅开始排队时不再误报“匹配成功”，重新登录或校准状态变化也不会绕过判断。
- 新增估算进本的记录时保留空耗时；再次编辑并修改结束时间时也继续保留，避免排队时间进入平均耗时统计。
- 校准诊断明确显示累计进度，以及最近识别到的登录和进本时间；重新登录后保留已有勾选，提示不再把旧的进本证据误说成当前仍在副本中。

## [0.7.9] - 2026-09-13

### 界面
- 手动修正优先展示实际结果与保存后的成就进度变化，可直接选择“已通关，计入进度”；明确说明取消、未通关或未勾选计入时为什么不增加次数。
- 修正弹窗改用不透明背景，时间详情与修改对比按需展开，保存按钮改为“保存修改”，修正原因提供可编辑的默认值。
- 缺少进本时间时，可主动选择按匹配时间估算；自动保留估算说明，并将实际耗时留空，避免影响平均耗时统计。

### 修复
- 校准时，大型普通报文可能挡住出本识别，导致已经回到外面仍显示“出本 · 还没见到”。现在同一连接此前至少两次换区中各出现一次、且包含已知副本证据的唯一换区标记与区域信息，可以辅助识别后续换区；仍须满足加载报文簇条件，候选不明确时继续观察。
- 早先漏认换区时，区域信息会被算成“换区之外”，原先会永久失去候选资格。现在采用与换区标记相同的有限容差，同时要求它在选定的进本、出本中各出现一次，让后续有效证据能够继续完成校准。
- 漏掉出本后重新登录，后续完整副本可以继续完成校准；不再永远等待旧连接上那次已经错过的出本。

## [0.7.8] - 2026-09-13

### 修复
- **抓包的内核缓冲从 1 MiB 提高到 16 MiB，不再丢失连接的第一个包。**读包线程与解码线程共用一把锁，进本瞬间的大批报文会使读包暂停数十毫秒，而 1 MiB 在百兆线路上仅够容纳约八十毫秒的流量。溢出部分由驱动直接丢弃，其中若包含握手包，该连接将无法再被解析，只能等待下一次重新登录。上一份真机报告丢弃 4799 个包，十二分钟内未读到任何一条游戏连接。

### 新增
- **诊断报告可区分“本软件启动过晚”与“用户已重新登录、但握手包被丢弃”两种情况。**此前两者提交上来的报告完全相同，都是一份 `messages_decoded: 0` 的报告。现在新增三项计数：`handshakes`（观察到的 TCP 握手次数，不区分程序）、`game_connections`（开始抓包以来系统为游戏记录的连接总数）、`game_connections_now`（当前连接数）。`game_connections` 超过 `preexisting_connections`，即表明本软件运行期间游戏确实重新建立过连接。
- **游戏进程的运行时长写入报告（`game.uptime_ms`）。**与抓包自身的运行时长并列后，可直接判断本软件是否在游戏启动之后才开始运行，无需询问用户或依赖用户回忆。

### 变更
- **不再提示用户重复执行无效操作。**当游戏在本软件运行期间新建过连接、却仍然读不出内容时，提示不再是"回标题界面重新登录一次"，而是"再登录一次也没用,先关掉加速器/VPN/正在占网的下载,重启本软件"。上一版本提示的重新登录并非问题所在。

## [0.7.7] - 2026-09-13

### 新增
- **诊断报告说明“本轮为何从零开始”。**此前只有 `carried` 一个数字。它为 0 时，文件不存在、文件来自其他版本、模板已更换、文件损坏四种情况无法区分。现在新增 `carried_source` 一行，取值为 `OK` / `NO_FILE` / `OTHER_BUILD` / `OTHER_TEMPLATE` / `OLD_LAYOUT` / `UNREADABLE`。已积累的证据未能延续时，该行直接给出原因。

## [0.7.6] - 2026-09-13

### 变更
- **副本先选择种类，四人本与八人本完全分开。**0.7.5 已将排序改为按种类、等级、加入游戏的先后，但四人本与八人本仍在同一张列表中，列表过长。现在列表上方新增种类选择，默认为四人迷宫，选定后再选资料片，列表缩减至十余条。打开已有记录时，种类与资料片自动切换到该记录所属的分档。
- **标题栏显示版本号。**版本号写在"导随记录器"之后，用户无需查看安装包或导出诊断报告便能确认所用版本。
- **职业下拉框移除九个基础职业。**剑术师、格斗家、斧术师、枪术师、弓箭手、幻术师、咒术师、秘术师、双剑师在三十级即完成转职，不会出现在记录中。现在只列出转职之后的职业。

## [0.7.5] - 2026-09-13

### 修复
- **修复 0.7.3 之后本软件既不记录也不校准的问题。**0.7.3 使"永远进不了本"的本机档案失效，方向正确；但校准接管的前提是"这个版本没有任何档案"，而此类机器上档案存在、只是不可用，两个条件不一致，校准因此始终未启动。现在"本机写出但已不可用"的档案会触发校准重新接管。
- **下拉列表不再被窗口底边截断。**上百条的列表此前会延伸到窗口之外，下半部分既不可见也无法点击。现在列表限制了高度并带有滚动条，下方空间不足时向上展开，打开时会把当前选中项滚动到可见范围内。
- **副本按游戏内顺序排列。**此前按名称的字典序排列，与任务搜索器中的顺序不一致。现在先按种类排列，迷宫、歼灭战等各自成块，不再互相夹杂；其次按等级，最后按副本加入游戏的先后。

## [0.7.4] - 2026-09-13

### 修复
- **修复抓包出错时日志丢失关键信息的问题。**真机日志中连续 11 次 `monitor_faulted`，每一次都记为 `error_type: null, error_message: null`，不含任何信息。原因是抓包的错误上报分两种：一种携带异常，另一种只携带一句说明。Machina 的监视线程通过回调报错，不抛出异常，而写日志的方法只记录异常，因此"没有异常"的那一类错误在日志中没有留下任何内容。现在两种上报均予记录。
- **诊断报告新增 `last_error_detail`。**仅有 `ERR_INTERNAL` 无法定位问题：该码同时代表监视器故障、采集源未释放、协议线程未退出等完全不同的情况。新增该字段后，无需再查阅日志文件。

## [0.7.3] - 2026-09-13

### 修复
- **修复职业下拉框过滤未生效的问题。**0.7.1 的过滤条件读取了错误的字段：表中表示"生产采集"的是 `role_group`（坦克/治疗/近战/远程物理/魔法/其他），而实现读取的是 `role`。`role` 是另一个字段，在本机数据中没有取值，因此没有过滤掉任何职业。
- **副本下拉框不再列出随机任务无法产出的内容。**金碟游乐场、深层迷宫、任务战斗、PVP、寻宝、假面狂欢均已移除，只保留四人迷宫、讨伐歼灭战、大型任务、团队任务、行会令。
- **机器上已存在的"永远进不了本"的档案现在会自动失效。**0.7.2 只是不再**写出**此类档案，对已有一份的机器没有作用：文件仍在、版本仍然匹配，因此继续生效，校准始终未启动。现在此类档案被判定为不可用，校准重新接管，补齐缺少的那条报文后将其覆盖。

## [0.7.2] - 2026-09-13

### 修复
- **修复副本结束后总览仍停留在「已匹配」、记录始终为"未知副本"的问题。**按排本申请推断的档案依靠"进入的是哪个区域"区分进本与传送。若本机未识别出"本次换区进入的是哪个副本"那条报文，该档案**无法判定进本**：排本后即进入"已匹配"，此后无论游玩多久都停留在该状态，记录也只能以"未知"收尾。此类档案的表现劣于没有档案，现在不再启用，校准继续查找，并明确指出仍缺少哪一项。
- 机器上已存在此类档案时，升级后校准会重新接管，补齐缺少的那条报文后再启用。

### 变更
- **手动修正的副本下拉框先按资料片分档。**八百余条一次性列出难以选择。现在先选择「2.X 重生之境 / 3.X 苍穹之禁城 / …」，再选择副本，每一档的条目数便于浏览；副本名前标注等级。打开已有记录时自动切换到该记录所属的分档。

## [0.7.1] - 2026-09-13

### 修复
- **语音不再在用户点击「申请」时播报「匹配成功」。**当前档案按排本申请推断，点击申请后立即进入"已匹配"状态，语音因此播报了并未发生的匹配。语音播报用于用户视线不在界面上的场景，一次误报即会损害其可信度。现在此类档案下不播报该句；进本与结束的播报照常，它们对应确实发生过的事件。总览上的状态同时改为「已排本,等待进本」。
- **换区报文的容忍度改为按比例计算，不再使用固定的两次上限。**固定上限带来的正是容忍度本应消除的问题：用户换区 28 次，标记在其中 18 次中各出现一次、簇外出现 3 次，比例上可以忽略，却比上限多 2 次而被否决。**游玩越多越容易被否决**，校准因此无法完成。
- **手动修正中的副本下拉框现在列出完整的副本表。**此前只列出已记录过的副本，而需要修正的恰恰是本软件未能识别副本的那一条，因此只能选到"未知副本"。
- **职业下拉框只列出战斗职业。**此前列出全部 43 个职业，包括刻木匠、锻铁匠、采矿工等，这些职业不会出现在副本记录中。

## [0.7.0] - 2026-09-13

### 新增
- **点击「错」之后，可直接指出本次实际排取的随机任务。**此前"错"只有一种含义，且代价最大：判定为本软件识别错了报文，候选作废并重新观察，需要重新积累一晚的证据。而 2026-09-13 实际出错的只是**名称**：用户排取的是顶级迷宫，时间线显示"高难度任务"，报文识别完全正确。仅因名称错误而作废整晚的证据并不合理。
- 现在点击「错」会追问"那这一把实际排的是"，由用户从随机任务列表中选择一项。该记录照常计入，校准不作废，本软件把所选名称记在它读到的那个编号上，此后一直沿用。只有选择「这个时间我根本没排本」才真正否决这一条，与此前一致。
- 改正内容保存在 `%LOCALAPPDATA%\MentorRecordereferenceoulette-names.json`，跨版本保留。删除该文件后恢复随包名称。该内容仅用于显示，不参与任何判定，也不写入协议档案。
- 下拉框中的名称取自内置于本软件的游戏任务列表，而非随包名称表。使用该功能的前提正是随包名称表可能有误，若改正所用的列表同样取自该表，用户将无法选出正确的名称。

## [0.6.1] - 2026-09-13

### 修复
- **修正随机任务名称错误。**5、6、8、17 四个编号在名称表中对应了错误的名称：申请顶级迷宫时，时间线显示"高难度任务"；申请满级迷宫时，时间线显示"大型任务"。表中的名称取自公开的 CSV，但对应到了错误的编号上。现在改为本项目**实际在流量中观察到**的对应关系：1 练级、2 拾级、3 主线、4 行会令、5 顶级、6 讨伐歼灭战、8 满级、9 指导者、15 团队、17 大型。未观察到的编号不予收录。
- 该表仅用于显示，不参与判定，也不写入协议档案。名称出错的那几次，记录本身并无错误。
- **「离开副本」一行现在写明它结束的是哪一次进本。**此前该行只显示"离开副本"，而其上一行可能是用户在副本进行中排取的下一个副本，容易被误读为"两分钟完成一个副本"。

## [0.6.0] - 2026-09-12

### 变更
- **窗口四角改为圆角。**圆角由 Windows 通过 DwmSetWindowAttribute 实现，因此阴影、贴边预览、任务栏预览一并为圆角，而非仅画面内容为圆角、外框仍为方角。Windows 10 及更早版本不支持该属性，会直接忽略并保持方角，不影响使用。
- **顶部的橙色提示条移入页面内部。**它原先位于标题栏上方，形似窗口的组成部分而非提示信息；改为圆角后又使窗口顶部变为平直。现在改为浮于页面右下角的卡片，文字与按钮不变，并新增「收起」。收起后，本软件有新的提示时会再次出现。
- **滚动条不再覆盖卡片。**每个可滚动页面的右侧留出 16 像素空位，滚动条置于该空位内，不再遮挡卡片右边缘。

## [0.5.7] - 2026-09-12

### 修复
- **已积累的校准证据不再被空证据覆盖。**证据落盘后出现了一种新的失败方式：本轮未能读入磁盘上的证据时（文件不可读、模板不匹配或其他原因），观察从零开始，而定时保存会把这份空结果写回文件，永久抹去此前的证据。真机上 76803 条报文、10 次换区即因此变为 922 条、1 次。现在新证据少于磁盘上的证据时不予写入。点击「重新观察」不受影响，该操作本身会先删除文件。
- **抓包接入已有连接（MIDSTREAM）时，校准卡片不再给出操作提示。**0.5.5 加入的抑制条件判断了错误的状态：MIDSTREAM 属于"受阻"分支，而非"在听但没声音"分支，因此该条件未生效，出现抓包半小时零报文、卡片仍显示"在游戏里申请一次随机任务"的情况。现在两种状态均纳入抑制。

### 新增
- 诊断报告新增 `carried`，表示本轮观察从上一次运行继承的报文条数。该值为 0 而磁盘上本应存在证据时，可据此判定为证据未读入，而非用户未进行游玩。

## [0.5.6] - 2026-09-12

### 修复
- **偶然匹配的客户端报文不再使整个校准回到初始状态。**本版本游戏的一次排本申请会返回**两条**回执。此前第一条回执消耗掉该次申请，第二条转而另行匹配，于是配上了用户按下按钮时客户端附带发出的、恰好以同一数字开头的另一条 24 字节报文。而"同一条回执下出现两个申请 opcode"当时被视为歧义，直接否决整条线索。真机上 `0x023c->0x021d` 匹配 4 次、`0x03bc->0x021d` 匹配 1 次，排本报文、匹配弹窗、进本三行同时退回"没看到"。证据落盘后，这一次巧合还会影响之后的每一次会话。
- 两处均已修正：申请不再被消耗，改由回执时间窗兜底；已匹配过的 opcode 组合优先。判定改为按**多数**决定，4 比 1 判为巧合，2 比 2 才判为歧义，后者仍如实报告为歧义。
- `zone_candidates` 此前按"从不在换区之外出现"计数，与本软件实际采用的规则不一致，会出现候选合格却显示 0 的情况。现在按同一套规则计数。

## [0.5.5] - 2026-09-12

### 修复
- **抓包无法解出任何报文时，校准卡片不再提示用户游玩副本。**真机上抓包中途接入了已建立的连接，此后半小时未解出任何报文，而校准卡片仍显示"打完这把副本，离开后再回来"，该状态下的游玩不会被观察到。现在卡片改为提示：先按本页另一条提示解决抓包问题，在此之前游玩副本不会推进校准。

## [0.5.4] - 2026-09-12

### 新增
- **校准证据保存至磁盘，升级本软件不再清零。**此前证据仅保存在内存中，关闭本软件或安装新版本后全部丢失，需要重新积累一晚的证据；而该阶段本软件几乎每晚发布新版本以定位当前版本的报文，证据因此反复丢失。现在证据写入 `%LOCALAPPDATA%\MentorRecorder\calibration\`，按区服与客户端版本分文件存放，下次启动后继续累积。
- 文件中记录了当时所用模板档案的哈希。游戏版本或模板发生变化时，旧证据直接忽略，不会被混用。
- 点击「重新观察」会一并删除该文件，校准完成并写出档案后同样删除，已清除的证据不会在下次启动时重新出现。
- 写入的内容与诊断报告中已输出的内容一致：opcode、长度、字节偏移、id 类数值、脱敏连接标签与时间，**不含任何负载字节**。文件不可读或内容损坏时一律视为没有证据，不影响启动。

## [0.5.3] - 2026-09-12

### 修复
- **换区标记不再因一次「在换区之外出现过」而被永久否决。**换区簇的边界由本软件划定：自第一条大报文向前只取 3 秒，因此某次加载的标记若到达更早，会被该次加载记为"簇外"。换区次数越多，遇到这种情况的概率越高。真机上一次**完全干净**的抓包（零丢包、零残留连接）进行了两把副本、九次换区，反而没有任何候选，而此前仅三次换区的会话可以识别出候选。
- 现在允许少量簇外出现，最多 2 次，且需与其标记过的换区次数成比例。长度仍取自模板，仍必须在所选的进本簇与出本簇中各恰好出现一次。放宽的只是边界条件，不是判定行为：高频出现的报文远超该额度，仍然不会被采用。
- 无法匹配时的提示改为如实说明，并给出可改变结果的操作（再完整进行一次进本出本、中间传送两次），不再显示"这一版需要新版本的软件"。

### 新增
- 诊断报告新增 `zone_shapes`，给出换区报文该长度上每个形状的 `恰好出现一次的簇数/总簇数+簇外出现次数`。`zone_candidates` 为 0 时，该行直接指明属于哪种情况。

## [0.5.2] - 2026-09-12

### 修复
- **不再把"抓包丢了报文"报告为"这一版需要新版本的软件"。**换区报文只要在换区之外出现过一次即永久失去候选资格。抓包完整时该规则成立，发生丢包时则会导致误判：某次换区的报文未收全就不会被识别为一次换区，其中的标记因此被记为"在换区之外出现过"，真正的标记被抓包自身的缺口否决。真机上同一版本此前两晚均识别出候选，当晚未能识别，却得到了"需要新版本的软件"这一无法处置的结论。
- 现在此类情况如实说明：本轮证据因丢包失效，重新登录游戏后点击"重新观察"重新开始。只有该长度上**没有任何形状**时，才判定为本版本更换了报文长度。
- 诊断报告新增 `zone_outside`，表示换区报文该长度上既在换区簇内、又在簇外出现过的形状数量。它与 `zone_candidates` 配合，可区分"丢包"与"报文长度确已变更"。

## [0.5.1] - 2026-09-12

### 修复
- **本软件在游戏登录之后才启动时会明确提示。**游戏此时已建立的连接无法读取，该段游戏过程既不会被记录，也不计入校准证据，卡片会持续显示"还没看到"。现在顶部直接给出说明：保持本软件运行，回到标题界面重新登录一次。
- 提示"再申请一个不同的随机任务"改为明确说明：**提交一次申请即为足够，看到排队后可立即取消，无需完成副本，期间请勿关闭本软件**。

### 新增
- 诊断报告的校准证据补充时间信息：`watched_seconds`（观测器实际观察时长）、`quiet_seconds`（距最后一条报文的时长）、`clusters_at` 与 `pairs_at`（每次换区、每次排本配对发生在多少秒之前）。此前一份"只看到一次排本、一次换区"的报告无法区分用户未进行游玩与报文未到达，现在可以区分。以上均为相对秒数，不含任何绝对时间。

## [0.5.0] - 2026-09-11

### 新增
- **不再假设「匹配成功」报文中随机任务编号所在的字节。**此前所有扫描只检查模板声明的那一个位置，即第 16 字节。本版本游戏把回执与匹配拆分为两条报文，新的那条是模板从未出现过的结构，编号位置未知。位置判断错误与"报文根本没来"在报告中表现完全相同，真机上连续四份报告因此无法推进。
- 现在每一条服务器报文的**每一个字节**都与当前正在排取的随机任务比对一次，按（报文、长度、第几个字节）记账。判据只有一条：该位置每次都携带**当时排取的编号**，并且覆盖过**至少两个不同的随机任务**。普通报文无法持续跟随一个变化的数值。该方法不需要拒绝匹配，也不依赖偶然。
- 诊断报告新增 `markers` 一行，列出该表中最有可能的八个位置，便于在校准停滞时定位缺口。

### 变更
- **无法识别匹配报文时，改用客户端发出的排本申请进行记录。**"匹配成功"完全可能是一条不带随机任务编号的报文，此时无论进行多少次游玩都无法找到它。与其始终不生成记录，不如按「申请了哪个随机任务」加「随后进入了哪个副本」记录。这是推断而非观察，因此在各处均标注为推断：档案中 `CONTENT_FINDER_POP` 的方向写作 `CLIENT_TO_SERVER`；匹配窗口放宽到一小时，该窗口衡量的是排队时长；状态机对此类档案额外要求**只有副本表认得的区域才算进本**，否则排队期间传送一次城即会被判为进本。
- 此类档案生效后**校准继续进行**：后台持续查找真正的匹配报文，找到后再次请用户核对并自动替换。卡片此时显示"已经可以正常记录导随了，软件还在后台找更准的判定依据"。
- 已知且唯一的代价：申请随机任务后取消，并在随后一小时内手动进入一个副本，该次记录会被归入已取消的那个随机任务。协议中没有任何信息可以区分这两种情况。

## [0.4.1] - 2026-09-11

### 修复
- **校准不再要求用户拒绝一次匹配。**游戏对拒绝次数有限制，次数用尽会被禁止排本，将其作为校准的必要步骤并不合适。
- 改为按报文出现的位置区分：属于区域加载的报文随每一次换区一起出现，登录、传送、进本均会出现；宣布匹配成功的报文不属于任何一次加载，也从不出现在换区报文簇内部。因此在任何一次换区簇中出现过的报文种类，一律不作为匹配报文的候选。
- 拒绝匹配降级为并列时的最终裁决手段，仅在上述判据仍无法区分两条报文时使用。正常情况下**只需正常游玩副本**。

## [0.4.0] - 2026-09-11

### 修复
- **本版本游戏把「排队回执」和「匹配成功」拆成了两条不同的报文，本软件原先认定它们是同一条报文的两种状态，因此始终等不到匹配。**真机证据：回执报文每次申请只返回两条，此后不再出现，无论用户完成副本还是拒绝匹配均是如此；而模板长度的报文中没有任何一条符合弹窗的形状。
- 现在回执报文中等不到匹配时，将「匹配成功」作为**独立报文**学习：它须在同一连接上晚于同轮盘的已配对申请，在回执窗口与加载之外到达，并在匹配窗口内先于**每一次**进本。仅凭这些条件仍不充分，因为进本前后本就有报文伴随，所以并列时还要求它**出现过而其后没有任何进本**：用户拒绝匹配时它照常发出，而伴随加载产生的报文在没有加载时并不存在。仍然并列时不予启用。
- 旧结构的版本仍走原有路径，行为完全不变：只有回执中确实等不到匹配时才去查找独立报文。
- 学习到的独立报文按观察到的 opcode 与长度写入档案，且不带状态选择器。模板中的状态字段描述的是回执报文的状态，而独立报文出现本身即代表状态。本机档案的证据说明会如实写明所用的路径。

## [0.3.9] - 2026-09-11

### 新增
- 真机上完成两把副本后，`match_echoes` 中仍有两条报文并列 2/2。进本前后本就存在大量报文，其中任何一条都可能恰好在该位置带有 1 到 17 之间的小数值。本版本为该行增加两项更细的判据。
- **距离**：每条候选给出它出现在加载开始前多少秒。宣布"匹配成功"的报文需要等待用户决定并等待读条，因此距离在数十秒以上；仅伴随加载出现的报文则接近 0 秒。
- **独立性**：`+N` 表示该报文出现过 N 次而其后没有任何进本。匹配成功的报文无论用户接受还是拒绝都会发出，伴随加载的报文在没有加载时并不存在。**拒绝一次匹配便可区分两者。**
- `roulette_echoes` 的每一条补充"上次出现在多少秒之前"。据此可进行一次五分钟的验证：排取一个随机任务，等待弹窗出现后拒绝，立即导出报告，并记下弹窗出现在约几秒之前。读数吻合的那一条即为目标报文，无需完成任何副本。
- 以上仅为提供给维护者的线索，不参与任何判定。报告中仍然只有报文种类、长度、计数与相对秒数。

## [0.3.8] - 2026-09-11

### 新增
- 脱敏校准诊断新增 `match_echoes`：将 0.3.7 的线索按时间再次筛选，记为 `这条报文在几次进本之前出现过/总共进本几次`。随机任务编号是 1 到 17 之间的小数值，约每一百条服务器报文就有一条恰好在该位置带有此类数值，因此仅凭数值几乎不构成证据。真正宣布"匹配成功"的那条报文必定在进本前两分钟内到达，且**每次**进本均如此。完成两把副本后，读数为 2/2 的那一条即为目标。
- 扫描保留的时间点按报文种类分别限量，高频报文不会占满表容量，低频报文可以保留。

### 修复
- 无。本版本不改变任何判定，也不改变本软件开始记录的时机。

## [0.3.7] - 2026-09-11

### 新增
- 脱敏校准诊断新增 `roulette_echoes`：统计**任意长度**的服务器报文中，在模板的随机任务编号位置带有用户实际申请过的编号、且已超出回执时间窗的那些，按报文种类与长度计数。真机上用户看到两次匹配成功弹窗并全部拒绝，而配对确认的那条报文只发出了六次申请应得的十二条回执，没有多余；同时没有任何一条模板长度的报文符合弹窗的形状。可见本版本的"匹配成功"不在模板的长度上，按长度查找无法找到。该项按报文种类的命中比例排序，命中率高而总量小的那一条即为目标。
- 该项仅为提供给维护者的线索，不参与任何判定，也不改变本软件开始记录的时机。报告中仍然只有报文种类、长度与计数，不含随机任务编号、副本名或报文正文。

## [0.3.6] - 2026-09-10

### 修复
- **人数密集的地图上，校准无法识别进本。**换区判定要求"进本前五秒没有任何大的服务器报文"，而周围存在其他玩家时大报文持续到达，该安静期无法出现。真机上完成两把副本只识别出三次换区，其中没有任何一次副本。此外，漏识别的那几次换区中的报文会被记为"在换区之外出现过"，从此永久失去候选资格，换区标记与区域报文的候选数因此归零，继续游玩也无法完成校准。
- 现在只要求"进本前五秒没有超大的服务器报文"。超大报文仅在加载区域时发出，不受周围人数影响；加载进行期间同样不会重复开启一次换区。
- 同时按报文间隔切分出真正属于本次加载的区段，人数密集的地图上换区时刻不再被此前的普通报文提前。

## [0.3.5] - 2026-09-10

### 修复
- **完成一整把随机任务后，校准卡片四行仍全部显示“还没见到”。**“进本”与“出本”判断的是“这次换区能不能佐证匹配弹窗”，而非是否进入过副本。没有弹窗样本时两者恒为否。现在只要观察到进入已知副本的换区，“进本”一行即显示“已看到，还没能和这次排本对上”。
- 已进入过副本时，提示不再要求用户等待已经发生过的“这次匹配成功”，改为建议再申请一个不同的随机任务。

### 新增
- `CaptureStatus.calibration.progress` 增加可选字段 `duty_zone_seen`，表示观察到进入已知副本的换区，与匹配弹窗是否对得上无关。旧版本 Collector 不发这一项，桌面端按缺席处理。
- 脱敏校准诊断增加 `duty_zones` 与 `territory_candidates` 两个计数，用于区分“还没打够”与“这一版把区域报文也挪了、继续打没有用”。报告中仍然只有计数，不含区域编号、副本名、连接标签或报文正文。
- `docs/capture-diagnostics.md` 新增 §9.4，说明校准停滞时 `calibration.evidence` 各字段的读法。

## [0.3.4] - 2026-09-10

### 修复
- 本机校准从有序的“排本请求 → 候选状态 → 已知副本进入”证据链学习匹配选择器，保留模板的普通值字段约束。迟到回执、普通传送、请求之前的状态及无新证据链的出本后状态不会被当作匹配。
- 换区标记必须与模板长度一致、簇外为零、在至少三个簇中各出现一次，并支持同一组进本和出本事件。大厅握手不再否决真实标记，大厅专有报文也不能冒充标记。
- 区分换区簇的三秒回捞范围与实际加载起点，避免快速进本时误排除真实弹窗，或把进本时间显示到弹窗之前。
- 候选证据表溢出或无法安全压缩时阻止启用草稿。停止捕获后刷新草稿缓存，保证状态、事件与证据计数一致。
- 校准卡片将仅有形状线索的状态显示为“尚未确认，等待进本核对”，并在进度变化时及时刷新。

### 新增
- 脱敏校准诊断增加形状数量、拒绝字段、已配对 opcode 的长度与选择器计数，以及簇内恰好一次的形状统计。报告不包含报文正文、连接标签或事件时间线。
- `CaptureStatus.calibration.progress` 增加可选诊断字段 `pop_shape_seen`，兼容缺省字段；该字段不代表已经确认匹配。

## [0.3.3] - 2026-09-10

### 修复
- **临时目录被游戏程序副本占满，进而影响系统。** 为解压游戏流量，本软件会把游戏程序
  （52 MB）复制到临时目录再加载（docs/privacy-boundary.md §4）。抓包出现"启动成功、随即崩溃"时，
  该副本未被登记，而清理只处理登记过的文件，因此每次重试都残留 52 MB。同时，故障退避
  在"启动成功"的那一刻即被重置，此类崩溃每次都会触发重置，重试间隔因此始终为 30 秒，真机日志中
  连续出现 355 次。系统盘被占满后，Windows 录屏首先失效，整机随之卡顿，关闭本软件后恢复。
  现在清理改为扫描 Machina 自身的临时目录，删除其中未被占用的副本，只删除以 GUID
  命名的文件，并在采集服务启动时与每次停止抓包时各执行一次。退避改为会话实际运行满一分钟才
  重置，异常环境下按 30 秒→1 分→2 分→5 分退避。自检报告直接列出该目录的文件数与占用字节。
- 校准进度不再出现"报告里已看到、卡片上还没看到"的情况。进度或阻塞原因变化时 Collector 会通知界面，
  界面每两秒的状态轮询同样会传递给校准卡片。
- 进本判定不再要求换区报文与匹配弹窗位于同一条连接。客户端同时保持多条连接，本项目尚无证据
  表明弹窗由哪一条承载。同连接仍然优先，但不再是硬性条件，否则实际已进本也可能始终无法确认。
- "报文种类超出上限"的提示改为指向「重新观察」按钮。重新登录会保留证据，因此也保留了溢出状态，
  只有「重新观察」才真正重新开始。两条报文均疑似弹窗时，同样明确给出「重新观察」这一处置方式。
- 频次上限不再淘汰已被真实弹窗印证的候选。排队时间较长时，副本查找器的状态报文本就会重复出现。
- 校准卡片的四行进度改为"排本 · 已看到 / 还没见到"，下方的红色文字只说明后续操作，两处表述不再冲突。

## [0.3.2] - 2026-09-10

### 修复
- **校准证据跨登录累积。** 此前每次重新登录都会开启一个新的抓包会话，校准观测器随之重建，
  此前记录的排本回执与换区簇全部清零，卡片重新显示"还没见到排本"。现在同一版本上的
  后续会话由同一个观测器接手，连接按会话分别打标，不会跨会话误配对。推导改按本机时钟
  而非会话相对时间，事件编号同样按时钟生成。只有"重新观察"才真正清空。
- 校准卡片四行进度改为"排本 · 已看到 / 还没见到"的写法，不再与下方的阻塞说明冲突。
- 脱敏诊断报告的 `calibration` 新增 `evidence`（会话数、报文数、配对/弹窗按 opcode 计数、簇数、
  换区候选数、簇外种类数、溢出）。Collector 日志在校准状态变化时记录同一份摘要，
  便于排查。

## [0.3.1] - 2026-09-10

### 修复
- **补丁后的客户端无法解出流量。** Oodle 解压签名档案按 exe 哈希精确匹配，游戏更新后必然不匹配，
  于是退回 Machina 内置签名表；而内置表在国服客户端上找不到 `OodleNetwork1TCP_Train` /
  `UDP_Train`，抓包因此无法解出任何内容，校准始终显示"没见到排本"。现在没有精确匹配时，取同区服
  最新的 VERIFIED 签名档案作为模式捐赠者，把 12 条通配模式在新的 exe 上重新扫描一次：全部恰好命中一次
  才使用，不再比对旧 RVA；任一条未命中则照旧退回内置表，不因此拒绝抓包。诊断中的
  `oodle_signature_source` 新增 `pattern-fallback`。
- 校准状态新增 `WAITING`。版本没有档案、已有模板、但抓包尚未运行时（包括刚点击"重新观察"而抓包
  已停止），不再显示为"协议档案不匹配"，横幅改为"准备重新校准，正在等待抓包启动"。抓包上次
  失败时一并给出错误信息，避免被误认为校准正在进行。
- 总览页"当前导随"卡片：校准提示不再将四项进度合并为一句话，文字最多两行，不再将下方的
  网格挤入标签。没有场次时职业列只显示"—"，不再绘制问号图标与空的职业徽章。
- 侧栏"捕获"在校准中显示"校准中 / 待核对"，不再显示"检查中"。
- 桌面端：Npcap 缺失的提示不再被校准提示覆盖；点击"重新观察"后卡片上的进度、时间线、阻塞原因立即清空；
  失败回退文案不再附带错误码；校准期间协议档案卡与校准卡片的文案保持一致。

## [0.3.0] - 2026-09-10

### 新增
- **本机校准（自学习档案）。** 游戏补丁重排 opcode 之后，本软件不再仅报告"协议档案不匹配"
  并停止工作，而是自动进入校准：以随包档案的报文结构为模板，在新版本的被动流量中重新识别
  排本请求与回执、匹配弹窗、换区标记、区域与职业报文。用户正常完成一把随机任务，出本后
  核对"排本 / 匹配弹窗 / 进入副本 / 离开副本"四项，全部属实即在本机写出一份 VERIFIED
  档案，并**在当前会话内**开始自动记录，无需重新登录，也无需等待新版本。校准期间不保留任何
  报文正文，只在报文到达时按结构取出轮盘、区域、职业编号。任何一项标记为"错"则本次作废，
  被否决的候选不再提出。报文结构本身发生变化时明确告知"需要维护者"，并保留原有的候选验证
  与导出证据路径。（docs/plans/self-calibration.md、docs/protocol-profile-format.md §11）
- 协议档案新增可选 `calibration` 模板段，仅 VERIFIED / SYNTHETIC 可携带，VERIFIED 须附证据。
  `cn.2026.08.05` 补充模板段与证据并重新盖章。本机档案存放于数据目录的 `protocol-profiles\` 下，
  随包目录中出现同版本档案时自动让位，每区服保留最新 3 份。
- IPC：`ConfirmCalibration` / `DiscardCalibration`，`CaptureStatus.calibration` /
  `profile_origin` / `calibration_bound_at_utc`，设置项 `auto_calibration_enabled`（默认开），
  实时事件 `calibration_changed`，错误码 `ERR_CALIBRATION_NOT_READY` / `ERR_CALIBRATION_REJECTED`。
- 随包数据新增轮盘名称表 `data/roulettes/`，仅用于校准对话框显示。
- `scripts/verify.ps1` 新增一步，用 Python 校验器检查随包目录下的全部协议档案。
- 界面：侧栏"协议"新增"校准中 / 待核对 / 本机校准"，捕获页新增校准卡片与核对对话框，
  设置页新增"游戏更新后自动校准"开关。

## [0.2.4] - 2026-09-09

### 修复
- 网卡列表在本软件启动之后发生变化（打开加速器 / VPN、更换 Wi-Fi、重新获取 IP）时，推荐的
  网卡可以正常打开：每次开始监听都重新枚举一遍网卡。所选网卡不存在时提示"网卡列表
  已经发生变化，正在重新挑选承载游戏流量的网卡"并按秒重试，不再误报为"Npcap 未安装、
  请重装"并退避 30 秒。
- 关闭本软件时改为先请求后台采集服务自行停止，最多等待 3 秒，再兜底结束该进程。退出时因此会正常
  关闭本次抓包记录、写下停止日志，并删除为解压流量而创建的游戏程序临时副本。
- 游戏程序的临时副本改为登记在数据库旁的清单中。本次未能删除的副本（本软件被强制结束、文件仍被
  占用），在下次启动采集服务时按清单删除。自检报告新增"游戏程序临时副本"一节，如实报告剩余
  份数与占用字节。该节只统计本软件登记过的文件，不扫描临时目录。
- 进本播报不再播报为"进入 未知副本"，状态变化事件现在携带该条记录本身。
- 重新连接同一个后台服务时不再重复播报、重复查询历史事件，去重改按事件编号进行。
- 每日自动备份改到首次连接后台服务之后执行。启动时不再弹出"备份失败：Collector 未连接。"，
  也不会因该次失败而在整个会话内不再重试。
- 复用已有实例时，每一次连接失败不再启动一个新的后台进程，而**常规交接恢复为无感知**：
  上一个实例退出、其 `serve.pid` 一并消失时，第一次连接失败即直接启动自己的采集服务，
  不弹出任何提示。只有"`serve.pid` 仍指向一个存活的占用者"才会计数到 4 次，且此后也不会
  立即结束该进程：先置位它的停止事件并等待 3 秒，其间只要它自行退出或管道恢复即停止处理；
  写入时间不足 15 秒的 `serve.pid` 一律不处理，该状态对应的是正在执行数据库迁移；只有等待期满、
  且四次失败均已计满，才依次执行 `tasklist` → `taskkill` → 再等 2 秒 → `taskkill /F`。
- 结束占用者是否成功改按命令的**退出码**判断，不再依据输出文本。中文 Windows 上 taskkill 的
  失败提示中没有 `ERROR` 字样，此前一次"拒绝访问"会被判定为成功，于是提示"已结束它并重新
  启动采集服务"而实际上未执行任何操作。进程核对同时改为逐字段解析 `tasklist` 的 CSV，要求进程号
  与映像名同时命中。整个接管过程改为异步执行，不再阻塞窗口。
- 结果确认询问不再被无限期推迟。对话框因正在保存另一条心得而丢弃的询问，会在该窗口关闭时
  重新提出，无需等到下一条记录结束。此前在本次会话没有下一条记录时，该询问不会再次出现。
- 游戏启动后自动记录卡片立即刷新，不再最多滞后 10 秒。
- 副本内掉线现在会记为"掉线"。此前该结果没有任何产生途径，掉线只会落入"中断"或
  "未知 · 待复核"。判定同时收紧两处：只有**最后一条**仍在解出消息的连接结束才计为掉线，
  国服客户端同时保持大厅、区域、聊天三条连接，聊天服务器自身的抖动重连不再把一场正常通关
  记为掉线；并且必须**游戏进程仍在运行**，关闭游戏仍然记为"中断"。
- 连接结束改为排在**已解码消息队列**之后处理。数据库繁忙时仍在队列中的通关结果，现在会
  先被解析、再由掉线收尾，不会出现"先记成掉线、随后的通关被终态挡掉"的情况。
- 队列溢出在发生时即把当前记录降级为"中断"，不再等到停止采集时才发现。收尾时放弃的
  积压不再被误报为"事件序列缺口"。
- 本机时钟与服务器时间不一致时，正常收尾的记录不再变为"未知 · 永远未完结"并在每次启动时被
  重新发现：报文时间统一取本机时钟，纯自动写入的时间线改为钳制而非整体回退。钳制只处理端点
  顺序，因此另加一道判定：纯自动写入的记录若结果为"已通关"、而进入或结束时刻为空，改记为
  "未知"并标记待复核，而不是原样写入这条自相矛盾的记录。
- 排到其他随机任务时，进行中的导随匹配立即按"进本前取消"收尾，随后的换区不再记为一场导随。
  该判断完全依赖报文中的随机任务编号，而导随本身的样本至今未采集到，因此按**低置信度**
  收尾并标记**待复核**。若偏移读取有误，代价是用户多确认一条记录，而不是一场导随被改写为
  取消。此类记录因此会出现在"待复核"计数中。
- 职业没有变化的播报不再反复写入数据库、追加事件并刷新界面。
- 进本之后才收到的区域播报，只有落在进本前后 10 秒内的那一条会被采纳。出本前播报的目的地
  不再覆盖本场副本。
- 抓包故障自动重试之后，第一条记录不再显示"职业未知"，上一段会话记录的**职业**会沿用。
  区域**不再**沿用：每段会话的计时都从零开始，上一段 3 秒处观察到的城内区域在新会话中
  会被视为"5 秒前刚播报过"，进而被当作本次进入的副本。
- 记录收尾后立即回到"空闲"，不再停留在"未知终态"直到下一条已声明报文到达。该次归一同样
  发出一条"回到空闲"的实时事件，且只发一次，仅监听事件而不轮询的界面不会遗漏。
- 副本名不再跨区服回退，国服记录不会再取到国际服的英文副本名。
- 幂等表按 24 小时清理之后重放同一个请求编号，返回明确的"请求编号已超出保留期，请换一个
  重试"，而不是笼统的内部错误。
- 设置页的开关与输入框在 2 秒轮询下不再回跳。
- 结果确认对话框：保存时遇到版本冲突会自动取回最新版本并重试一次；对话框正忙时收到的询问改为
  排队，关闭后重新弹出，不再永久丢失；同一条记录再次弹出时，已输入的部分心得会被保留，
  只有切换到另一条记录才清空。
- 数据库被其他程序占用时，界面不再被解析线程阻塞十余秒：实时抓包的写入等待从 5 秒缩短到 1 秒
  （三次重试约 3 秒），人工更正仍然等待 5 秒。本次调整同时降低了两处预算：仅调整 SQLite 自身的
  `busy_timeout` 无效，驱动层另有一轮重试，会持续到它自己的命令超时（5 秒）为止，三次尝试仍然
  接近 15 秒。
- 仪表盘读取失败时不再清空卡片，也不再按"基线 + 0"播报错误的次数。一次都未读取成功时改为
  播报不含数字的固定内容。
- 编辑记录对话框的副本 / 职业 / 结果三个下拉框不再保留上一条记录中修改过的值。
- 玩家视图不再出现 `COMPLETED`、`AUTO_NETWORK`、`content_id` 等术语，也不再显示协议档案与
  排队时间两行。维护者工具中显示的内容与此前完全一致。
- CSV 导出的防公式注入只作用于文本列（副本名、副本分类、职业名、心得）。日期、时间、结果、
  时长、各类编号原样导出，不再被添加撇号。
- 撤销一次更正后，该字段不再被永久锁定：撤销至**该用户首次修改之前的取值**即解除该字段的
  人工保护，后续的自动观察可以继续补齐它。参照物不是创建时的初值：职业、
  进入时间一类字段在被人工修改之前往往已由抓包补写过一次，若以创建初值为参照，撤销之后
  字段仍会被判为受保护，后续真实的职业变更将无法写入。修改历史本身仍然只增不改。
- 语音只在引擎空闲时切换音色，不再打断正在播报的语句。
- 游戏未运行时，自动记录的轮询间隔从 2 秒放宽到 10 秒，游戏出现后立即恢复。
- "重新扫描 FF14"现在会重新读取一次抓包状态，重试耗尽后的提示因此可信。
- 备份目录中遗留超过 1 小时的导出临时文件会在下次备份时清理，且只清理本软件自行管理的备份
  目录，用户自选的目录一律不处理。
- 后台抓包库的诊断行改为每类最多写 8 条，其余每 30 秒汇总为一条，一次丢包不再在数十分钟内
  滚掉当天日志。
- 设置页无法显示数据库路径时，回退文案改为真实文件名 `mentor_recorder.db`。等宽字体不再硬编码为
  `Consolas`，改为在本机已安装的等宽字体中选取第一个可用者。
- 其他：跟随定时器不再重入；`--log-dir` 指向不可写位置时明确报错并拒绝启动（退出码 3）；
  收尾超时不再泄漏数据库句柄，也不再产生未处理异常；进程编号被复用时不再误认游戏进程；
  夏令时回拨的那一小时内看门狗不再误判父进程已被回收；候选取证的默认导出目录跟随
  `MR_DATA_DIR`，文件名不再受系统日历影响。

### 新增
- 记录新增副本身份来源的标注（报文中的副本编号 / 由区域推断 / 人工填写），
  数据库 schema 随 `0008_run_duty_source.sql` 升到 **8**。该标注只保留在本机数据库中，不出现在
  界面、实时事件与导出中。
- 协议档案支持字段级 `role: "selector"`。标记为筛选器的字段取值不匹配时，该条报文计为
  "与记录无关"，不再计入"解析失败"。国服档案的 `finder_state` 已改为筛选器，一次完全正常的
  导随因此不再显示"失败 2"。
- Oodle 签名档案新增 `reproduction` 字段（`checked` / `unchecked`），记录该签名是否经过
  复现检查。没有该字段的旧档案照常加载。
- 采集服务新增退出码 **6**：通信管道被另一个采集服务占用，**且**该管道确实存在、仍可接受
  连接，但它连续 6 次、每次半秒（合计 ≥ 3 秒）都不回答 `GetVersion`。租约被占用但尚无管道
  （对方正在启动或正在停止），以及管道的实例已被占满（对方正在正常服务），均返回 **4**，
  对应的动作是连接该实例而非结束它。占用者的进程号写在日志目录下的 `serve.pid` 中。
- `serve.pid` 只在数据库迁移、崩溃恢复与管道服务全部就绪之后才写入，退出时则第一个被删除，
  正在执行迁移的采集服务因此不会被误认为"僵死的占用者"。使用 `--pipe` 指定其他管道名时，
  写入的是 `serve.<管道名>.pid`，不会覆盖或删除实际在用实例的进程号。
- 采集服务新增优雅停止事件 `Local\<管道名>.stop`，没有控制台的桌面端同样可以使其干净退出。

### 变更
- 研究模式的候选负载上限从 256 字节放宽到 **512 字节**（数据库 schema 随 `0007` 升到 7）。
  v0.2.3 的变更记录写作"仍为 256 字节"，在该 tag 上是准确的；本次放宽发生在该 tag 之后，此前漏记。
- 勘误：v0.2.3 的变更记录称 CI "运行 `verify.ps1` 的全部关卡"，实际 push / PR 上一直排除 Soak
  吞吐量套件，原因是共享 runner 没有稳定的吞吐基线。已发布段落按打包脚本的规则不再改动，在此更正。
- 打包脚本新增核对：`CHANGELOG.md` 中每个已有 `vX.Y.Z` tag 的段落必须与该 tag 上的内容逐字一致，
  否则打包与 `-Verify` 失败。新的变更只能写入 `[Unreleased]` 或下一个版本。
- 自动记录不再把由区域编号反推出的副本编号写入统计聚合列。副本统计改为按"报文中的副本
  编号，缺失时按区域编号"聚合。仅按区域识别出的副本仍然单独成行，名称取自本地副本表。
- 发布包不再包含任何 `.pdb`。Release 构建把调试符号内嵌进程序集并重写源码路径，崩溃堆栈仍带有
  文件名与行号。打包脚本新增"产物中不得出现本机仓库路径"的检查：反斜杠与正斜杠两种写法
  （CMake、qmlcachegen 与 Qt 记录的是 `D:/...`）各按 UTF-8 与 UTF-16LE 扫描一遍，共四组特征串，
  打包与解包验证各执行一次。
- `BUILD-METADATA.json` 的 `live_capture_status`、`packaged_verified_profile_status` 与
  `public_distribution_ready` 改为从**打包完成的产物本身**读出，不再取自脚本中的字面量。
  新增 `live_capture_status_source` / `packaged_verified_profile_status_source` /
  `public_distribution_ready_source` 三个字段说明每个值的来源。其中
  `live_capture_status` 与 doctor 自报的 `public_distribution_ready` 是**编译期常量**，
  即从产物二进制中读回的声明，而非对该产物的实测，控制台输出与阻断原因也按此措辞。
  真正可能与事实不符的三条前提保持不变：包内没有 VERIFIED 档案、工作区不干净、使用了 `-SkipVerify`。
  不满足公开分发前提时新增 `public_distribution_blockers` 逐条说明原因，`-Verify` 会拒绝
  与元数据不一致的产物。
- 安装器默认目录：只有 D 盘为本地固定磁盘且可用空间不少于 512 MB 时才默认 `D:\MentorRecorder`，
  否则回落到 `Program Files\MentorRecorder`。光驱、可移动盘、映射网络盘、空间不足的磁盘一律回落。
  目录页照常显示，用户仍可修改。
- CI 手动触发时新增"Run the Soak suite"选项。勾选后 `verify.ps1` 不带任何测试筛选运行，
  即执行全部关卡。push / PR 上仍然排除 Soak，原因是共享 runner 没有稳定的吞吐基线。
- 开发侧：桌面端测试直接运行 exe 时不再可能拉起使用者自己的 Collector，每个测试的
  `main()` 首行即关闭子进程启动，并把数据目录指向临时目录；`.gitignore` 与
  `scripts/build.ps1` 中指向已不存在的 `.superpowers/` 的说明改为实际位置；
  openspec 的证据索引如实标注哪些路径只存在于本机、不在版本控制中。
- 抓包验证取证文件具备明确的保留策略：最多保留最近 10 次会话、最长 7 天；界面发起的验证最长
  2 小时后自动停止。命令行 `--capture-trace` 未给出 `--duration-seconds` 时，仍然只受行数上限与
  Ctrl+C 约束。
- 抓包验证取证（`--capture-trace`）的每条消息行新增 `epoch` 字段，即报文所属压缩包的服务器
  时间戳。`at_utc` 自 0.2.3 起记录的是本机时钟，而更早的取证记录的是服务器 epoch，两代取证
  因此可以通过 `epoch` 对应。候选账本的时间戳同样改为本机时钟，但账本中没有可写入 `epoch`
  的列，增加一列需要一次迁移，因此与 0.2.3 之前的研究数据存在一处已知的时间基准断层。

## [0.2.3] - 2026-09-08

### 修复
- 导随结束后"无播报、无记录、次数不变、诊断里什么都没有"的整组原因：
  - 抓包：监听开始时游戏已登录（中途接入）、网卡选错（加速器 / VPN）、首包归属确认超时等情况原来
    会持续显示绿色"自动监听中"而接收不到任何数据。现在这些情况以琥珀色状态加中文提示显示
    （"监听开始时游戏已登录……请回到标题画面重新登录"、"所选网卡上没有游戏流量……请重新选择网卡"），
    并新增原始报文与首包计数；游戏可执行文件读取慢于 5 秒不再丢失连接前缀；单个丢包不再永久停止
    该连接的解码；抓包故障后自动重试，不再需要重启本软件；记住的网卡不再承载游戏流量时改推荐当前
    承载流量的网卡；游戏刚启动、尚无连接时按默认路由网卡提前开始监听。
  - 状态机：排本弹窗被他人拒绝后再次弹窗时，120 秒匹配窗口改从最新一次弹窗开始计时。此前晚于窗口进本
    会被记为"进本前取消"，随后的整场副本完全丢失。
  - 重启后恢复：以"未知 · 待复核"正常收尾的记录不再在下次启动时被改写为"中断"。
  - 一次弹窗紧接另一次弹窗时，新记录的第一条事件不再因去重被丢弃；数据库繁忙时对该事件重试而非丢弃。
- 语音播报：结束播报改由"记录结束"事件触发，此前紧接下一次弹窗时终态不会播报；新增"结束待确认"
  模板（默认"导随结束，请确认是否通关，已确认 {progress} 次"）；进本前取消不再播报"异常结束"；
  重连后不再重复播报历史事件；两条播报不再互相打断；语音引擎异步就绪后重新应用音色与音量。
- 详情面板的"确认复核"按钮此前会被 Collector 拒绝（字段未声明或无改动），现在可以正常使用。
- 人工更正过的字段不再被后续自动观察或重启恢复覆盖，按修订记录逐字段保护，其余字段仍自动补齐。
  只修改备注不再隐含"已复核"，待复核统计只依据待复核标记本身。
- 研究模式的推荐集新增"副本与职业识别"两条报文（区域初始化 136 字节、职业信息 16 字节），
  用于核对副本名与职业的字段偏移。负载上限仍为 256 字节。
- 捕获诊断：档案未声明的普通报文不再计为"解析失败"，改为单独显示"与记录无关"；链路正常时
  成功率不再显示 0.0%；玩家视图不再出现 opcode 等术语，仅在"维护者工具"中显示。
- 导出 / 备份请求超时从 8 秒放宽到 120 秒，大型数据库不再误报失败。每日自动备份只在成功后记为已完成。
- 心跳事件不再每 5 秒触发一轮完整刷新。开关与滑块控件不再在第一次操作后与设置脱钩。
- Collector：同一用户的第二个实例以退出码 4 明确报告"已在运行"，桌面端复用现有实例而非无限重启；
  8 个客户端占满管道不再导致 Collector 退出；诊断报告导出不再覆盖任意同名文件；CSV 导出防公式注入；
  备份清理只在托管目录内进行；幂等表按 24 小时清理；日志文件被其他进程占用时不再静默丢行；
  父进程编号被回收时不再误认。
- 测试与脚本不再写入或清理真实的 `%LOCALAPPDATA%\MentorRecorder` 日志与数据库，
  为此新增 `--log-dir` 与 `MR_DATA_DIR`。

### 新增
- 副本结束后弹出"本次导随结果"确认（通关 / 未通关 · 中途离开 / 稍后再说），一键确认后次数立即增加。
  仪表盘"待复核"区提供同样的一键按钮，并可在设置中关闭"结束后询问本次结果"。
- 自动记录现在可以填写**副本名与职业**。国服档案新增区域初始化（区域编号）与职业信息两条消息，
  记录卡片不再出现空白的副本名与"未知"职业。区域编号通过本地副本表反查名称与分类，
  本地表未收录的区域只保留编号，不做推测。这两条消息**不参与**进本与离开的判定，
  进出副本仍然只由已验证的 `0x014a` 决定。
- 协议档案新增可选消息种类 `ZONE_TERRITORY`。其 opcode 与长度已在本机抓包中复现，
  字段偏移取自 Sapphire 的公开结构体定义，尚未用本机负载样本核对，取得副本内采样后再行确认。

### 变更
- 版本号只来自 `Directory.Build.props`。打包时核对两个 EXE、元数据与变更记录是否一致，不一致即失败。
  安装包升级时清理旧的档案与插件目录，产物改名为 `MentorRecorder-<版本>-win-x64.zip`。
- CI 改为运行 `verify.ps1` 的全部关卡，包含桌面端与真实 Collector 的集成测试。

## [0.2.2] - 2026-09-07

### 修复
- 国服启动器以管理员身份拉起游戏时，普通权限运行的本软件读不到游戏安装路径，区服与客户端版本
  一直未知，协议档案停在 `NONE`，界面只提示"协议档案不匹配"。现在安装路径改从内核进程表读取
  （不打开游戏进程句柄），提权启动的客户端同样能识别。
- 桌面端在安装路径读不到时改为明确提示"无法读取游戏安装路径"，不再笼统提示档案不匹配。

## [0.2.1] - 2026-09-07

### 新增
- 常规启动即自动记录。桌面端打开后自行跟随游戏，从匹配、进本到离开全程无需手动点击“开始采集”。
- 统一的状态投影。等待游戏启动、检查中、正在自动监听、无法记录四种情况在界面与托盘上的表述一致。
- 无法记录时主动告警。协议档案不匹配一类的明确阻断会持续提示，并弹窗一次。窗口最小化到托盘时先发送系统通知，待用户打开窗口后再弹窗，同一故障不会随轮询反复提示。

### 变更
- 常规界面与托盘移除“开始/停止采集”与“开始验证”入口，历史、统计与手工订正仍属于历史查看工作流。候选取证等维护者工具改为显式启动选项。
- 正式捕获失败不再自动降级为候选取证。正式记录仍然必须匹配 VERIFIED 档案。
- 桌面可执行文件补充 Windows 版本资源（VERSIONINFO），资源管理器中可以看到版本号。

## [0.2.0] - 2026-09-07

首个面向公开发布的版本。

### 新增
- 国服 `2026.08.05` 正式协议档案 `cn.2026.08.05`：弹窗（S2C `0x0323`，第 16 字节为随机任务编号）
  与换区（S2C `0x014a`）两条消息，指导者任务编号 9。本软件可自动记录从匹配、进本到离开的全过程。
- 一键安装器（Inno Setup）：自带 .NET 与 Qt 运行时；默认安装到 `D:\MentorRecorder`，可修改；
  缺少 Npcap 时从官网下载官方安装程序并启动。
- 候选验证与正式档案并行。候选模式新增“副本内采样”，用于定位通关报文。
- 界面动效：页面切换、按钮、侧栏、提示条、成就环、对话框。
- 原创应用图标。

### 变更
- 通关判定尚未识别。离开副本的记录以“未知 · 待复核”收尾，由用户在记录中填写结果。
  `DUTY_RESULT` 改为档案的可选消息。
- 首次运行说明改为“注意事项 + 运行顺序”两段。
- 捕获诊断页只保留玩家需要的信息，验证取证、白名单、适配器表等移入“维护者工具”。
- 侧栏“协议”状态改用中文，游戏未启动时显示“待游戏启动”。
- 新增“设置 → 版权与来源”，含 SQUARE ENIX 素材声明、图标与名称数据来源、开源许可证。
  图标与名称数据依据 FINAL FANTASY XIV Materials Usage License 作非商业用途随包分发。
  图片不可用时界面回退为文字徽章，用户可在 `%LOCALAPPDATA%\MentorRecorder\icons\` 中自备图标。
- 随包附带 LGPL-3.0 与 GCC Runtime Library Exception 文本（`docs/licenses/`）。
- Collector 发布改为 self-contained。

### 修复
- 单方向抓包失败（登录后才开始监听）时的检测与提示。

### 已知限制
- 是否通关需要手动确认。
- 档案不区分副本区域，弹窗后 120 秒内的第一次换区被视为进入副本。辞退弹窗后立即传送会产生一条
  误记的记录，该记录同样以“待复核”收尾，用户可在记录中删除。
- 只支持国服 `2026.08.05` 客户端，其他版本 fail-closed，不予记录。
